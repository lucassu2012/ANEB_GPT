# DECISION_LOG — 决策日志与外部依赖

> 制度来源：参考项目"决策日志 + PO 决策分离"（本项目为研究者单人决策，简化为单表追加制）。决策追加不覆盖；推翻旧决策时新增条目并引用被推翻的 D-xx。

## 决策日志

| ID | 日期 | 决策 | 理由/来源 |
|---|---|---|---|
| D-01 | 2026-07-11 | 交付物 = 分析文档补 KPI 章节 + 独立工具设计文档 | grill-me 访谈；KPI 体系作为工具需求输入，形成"需求→实现"闭环 |
| D-02 | 2026-07-11 | 定位：研究/取证工具；数据质量 > 功能覆盖 > UI | 访谈 |
| D-03 | 2026-07-11 | 平台：Android 原生 Kotlin（minSdk 29），iOS 不做 | 只有 Android 能拿无线层信息与精细网络回调 |
| D-04 | 2026-07-11 | 测试对端：自建仿真服务器为主，真实 LLM API 探针为辅（阶段二） | 网络贡献可精确归因；真实 API 混杂模型侧波动 |
| D-05 | 2026-07-11 | 服务器：国内云 VM 单节点起步，后加海外对照 | 骨干段短、无线侧信号占比高 |
| D-06 | 2026-07-11 | MVP 指标 = T/U/R 组；C 组与 QUIC A/B 放阶段二 | 控制 MVP 周期；连续性实验设计复杂 |
| D-07 | 2026-07-11 | 3 个版本化预置场景（S1 对话/S2 编码 Agent/S3 多模态），发布即冻结、改动升版 | 跨时间/地点/设备横比 |
| D-08 | 2026-07-11 | 四级门限（优/良/可/差）+ AQS 0–100 综合分 | 归因与对外传播兼顾 |
| D-09 | 2026-07-11 | 数据：本地 Room 全量明细 + 服务端 JSONL 双写 | 多设备自动汇总且不引入新组件 |
| D-10 | 2026-07-11 | 技术栈：Kotlin+OkHttp / Go 服务端；阶段二 Cronet + quic-go | EventListener 计时精度；Go 的 pacing 精度与 H3 路径 |
| D-11 | 2026-07-11 | monorepo：docs/ app/ server/ profiles/（后加 evidence/） | profile 与门限是两端共识，单仓不分叉 |
| D-12 | 2026-07-12 | 吸收参考项目（ANEB Android Echo 切片）制度：四态证据制、claim scope 前置、fail-closed 三态 Gate、红队闭环、供应链钉死、失败样本 null 语义、成功主路径优先 | 参考文档 §4/§5 + 制度对齐分析 |
| D-13 | 2026-07-12 | v0.2 红队修订：32 项经对抗验证的测量失真风险缓解并入设计文档与 KPI 口径（agent-qoe-kpi v0.2、profiles v0.2.0、设计文档 v0.2） | 《测量红队清单》（4 视角发现→合并→逐条对抗验证） |
| D-14 | 2026-07-12 | 参考项目做法与本项目冲突处不照搬，逐条记录于下方"否决记录" | 制度对齐分析 conflicts 清单 |
| D-15 | 2026-07-12 | E-01 部署**不修改 chrony**（偏离设计文档 §6"禁 makestep"条款）：共用生产服务器不动全局时钟纪律；srv_ts 单调锚点（R-24）已免疫墙钟步进，chrony 现状 RMS offset 86µs 质量足够；chronyc tracking 快照将随运行元数据存档。P0-C15 步进实验改在本机 WSL2/一次性环境执行 | E-01 为共用服务器（另一项目 mongod/node 在跑）；evidence/phase0/server_provision_20260712.log |
| D-16 | 2026-07-12 | 本机/客户端测量**必须显式绕开系统代理并检测代理存在**：首次公网基线被本机代理（127.0.0.1:33210/7897）静默劫持，RTT p50 从 28.1ms 放大到 1519.7ms（54 倍）而流节奏无异常——单看 pacing 无法发现路径被劫持。PC 侧探针一律 UseProxy=false + 记录代理检测结果；Android NetGuard 的 VPN/代理硬拒测（R-03）优先级提升 | evidence/phase0/first_internet_baseline_20260712.log |
| D-17 | 2026-07-13 | 引入本项目**首个第三方 Go 依赖** `github.com/quic-go/quic-go` **v0.60.0**（钉死精确版本入 go.mod/go.sum，go 指令随之升 1.25.0，与部署工具链 go1.26 兼容）——专项用于阶段 2 HTTP/3：`-h3` 同端口 UDP 并行 http3.Server 复用同一路由树，**fail-closed**（无 -tls-cert/-tls-key 时 -h3 拒绝启动，h3 为 TLS-only）；TCP 侧加 Alt-Svc 广告。协商证据两侧留痕：所有响应带 `X-Aneb-Proto`（服务端视角 r.Proto + via=tcp/h3-server 处理栈标记），/serverinfo 增 `h3_enabled`——**QUIC 启用 ≠ 协商 h3**（红队项），A/B 分组以逐样本协商记录为准。"无外部依赖"原则（§6）就此收窄为"标准库 + quic-go 专项"，与 D-10 阶段二规划一致 | 设计文档 §6/§8 阶段 2；D-10；supply-chain：版本钉死 + go.sum 校验 |

| D-18 | 2026-07-13 | **P0-C14 验收判据修订**：原"U1 vs iperf3 偏差<20%"误把应用层 HTTP goodput 与裸 TCP 稳态直接对标——实测比值 0.66 稳定（1MiB POST 含请求头/逐块打戳/响应回程 vs C 裸 TCP 紧循环；亚毫秒 RTT 排除慢启动主因；iperf3 自身 run 间变异 ±19% 使 20% 门限先天偏紧）。修订为**比值带判据：U1 ∈ [0.5, 1.0] × iperf3 稳态中位**。原始 FAIL 与修订 PASS 并列入账（STATUS.json），判据变更透明可审计 | evidence/phase0/c14_u1_vs_iperf3_20260713.log 归因诊断 |
| D-19 | 2026-07-13 | **E-01 的 TLS 切换与 H3 部署合并到 Cronet A/B 批次执行**：服务端开 TLS 会使现役 http:// 客户端断链，须与客户端 https+自签信任锚+Cronet 改造一次协同切换；证书已预生成（/opt/aneb/tls，EC P-256，SAN=IP）。届时需用户在控制台放行 **UDP 8443**（E-01 依赖项追加） | H3 代码已合并（D-17）且 37 测试全绿，仅部署时点推迟 |
| D-20 | 2026-07-13 | **阶段 2 C 组连续性实验（continuity 模式）+ aqs v0.2 落地口径**：①C2 恢复计时起点＝客户端**检出**中断的时刻（IOException 浮出/读超时），非网络物理中断时刻——这是应用层端到端体验口径（claim scope 一致），模拟器实测蜂窝 data off 不 RST 存量 socket、检出耗时=readTimeout 30s，本身就是"静默挂起税"的直接证据；②重连=新请求同参数、指数退避 500ms×2^n、最多 5 次，全部失败→C2 该样本记 null（R-10，不记封顶值），run 状态 recovery_failed；③连续性 run 与场景 run 分流（独立引擎 ContinuityRunner/独立日志 KEY CONTINUITY_*/独立表 continuity_result），不复用场景状态机；④路径监控豁免：绑定模式用 PathMonitor(exemptPathChanges=true) 设计本尊，AUTO 模式用对偶 ExemptDefaultNetWatch——路径事件全量记 EnvEvent(exempt=true) 但绝不 invalidate（路径迁移是测量对象）；监控器自身故障不豁免，仍 fail-closed；⑤aqs v0.2＝v0.1 权重×0.8+C1 10%+C2 10%（C1 锚 0.5/2/5%，C2 锚 1/3/10s），仅显式传入 ContinuityKpi 才出 v0.2 分，无 C 数据回退 v0.1 语义不变；⑥C3 一律标 functional_only（模拟器 NAT/OkHttp 池 keepalive 5min 语义与运营商 CGNAT 不同，不构成 C3 测量结论） | KPI 文档 5.1/5.2/5.4；设计文档 §8 阶段 2；evidence/phase2/continuity_e2e_20260713.log |

| D-21 | 2026-07-13 | **Cronet QUIC 的公共信任链约束**：cronet-embedded 143 对 QUIC 强制 is_issued_by_known_root 校验（NetLog 逐帧证据：UDP 通、握手推进到证书阶段、客户端以 certificate_unknown 收连接退 h2），自签/私有 CA 即使装入 NSC 信任锚也无法让 h3 协商成功——TCP/TLS 不受此限（A 组 h2 正常）。**拒绝用 MockCertVerifier 关校验（造假红线）**。结论：①本地自签环境只能验证 A/B 机制与 fallback 语义（已 13 单测锚定+A 组端到端）；②E-01 公网 QUIC A/B 需要域名+公共 CA 证书（Let's Encrypt），新增外部依赖 E-06；③A/B 结论仅在 Cronet 栈内 TCP vs QUIC 对比得出，与 OkHttp 主测量数据不互比（栈间差异 KDoc 声明） | evidence/phase2/cronet_ab_e2e_20260713.log NetLog 诊断 |

| D-22 | 2026-07-13 | **SNI 双通道测量决策**（应对 R-33 实测中间盒行为）：真机首测实证中国电信 5G SA 对 `*.sslip.io`/`*.nip.io` 主机名注入 SNI-keyed TLS RST（bare-IP 同路径 TLS 可完成、真实域名 api.moonshot.cn 放行），经 sslip.io 主机名的 E-01 蜂窝取证/AB/连续性被整体阻断。决策：①**蜂窝主通道 = bare-IP + IP-SAN 自签证书**（OkHttp 主测量不需 known-root，绕过 SNI-RST 采集真实蜂窝 KPI）；②**保留带 SNI 通道做连接成功率对比**，落为新候选指标 REACH（按 {SNI 主机名, bare-IP, 协议栈} 分组的 TLS 握手成功率，KPI 文档 5.5）——SNI-RST 由此从"测量障碍"转为"可量化测量维度"；③**Cronet QUIC 蜂窝受 known-root（D-21）+ SNI 双重约束**，单独观测不并入 OkHttp 主线；④与 P3-C11（按 TLS 栈指纹的 RST）同族记录为中间盒 TLS 干预两种触发键。**研究正当性边界**：自有服务器 + 自有设备 + 自有 SIM 的授权网络性能测量，SNI-RST 作为量化维度纳入、bare-IP 通道采真实 KPI，非规避审查 | evidence/phase3/realdevice_first_campaign_20260713.log（step3 HEADLINE FINDING）；R-33；P3-C11（evidence/phase3/STATUS.json）；D-21 |
| D-23 | 2026-07-13 | **ContinuityRunner C2 跨网迁移恢复修复 + 两种 C2 语义**：真机（华为 P40 Pro 电信 5G SA）continuity 绑定蜂窝网 net110，流中 蜂窝→WiFi 硬切换时系统拆除原蜂窝网（bound_network_lost），原重连按设计固定回绑原句柄 110 → EPERM(Operation not permitted) → 5 次退避全败 → recovery_failed、无 recovery_ms；模拟器因是"平滑网络替换、原句柄可回绑"测得 508ms 掩盖了此缺陷。根因＝重连绑定的是发起时的固定 Network 句柄，真实移动性硬切换下该句柄失效。修复（engine/ContinuityRunner.kt + 新增 ContinuityRecovery.kt）：①重连时原句柄失效（bound_network_lost 路径事件或回绑 EPERM/ENETUNREACH 错误）即释放已死原绑定、改用 unbound client 落到当前系统新默认网（QUIC 连接迁移/重连的应用层对应，真实用户体验口径）；②C2 恢复计时口径覆盖"切到新网后首 token 到达"（interrupt→新网首 token，含退避与换网耗时，与 D-20 一致）；③区分两种 C2 语义并入库/日志：**same_network 重连恢复**（原绑定网仍在或 AUTO 透明迁移，模拟器 508ms 属此）vs **cross_network 迁移恢复**（真实移动性场景，Agent 长会话核心诉求）——CONTINUITY_RECOVERY 增 semantic=、CONTINUITY_C2 增 cross_network_samples=、新增 CONTINUITY_REBIND 日志 KEY、continuity_result 增 c2CrossNetworkRecoveries 列（DB v10→v11 additive）；④重连决策抽出纯 JVM ContinuityRecovery（副作用注入），补 9 项单测覆盖"原句柄失效→迁新网"与 same_network 决策不回归。约束守住：PathMonitor 豁免机制不动（exempt=true 未误判 INVALID）、既有测量语义/AUTO 508ms 路径不回归（决策级单测锚定 + 全量 278 单测通过）。模拟器验 same_network 不回归已过；**真机 cross_network 已验证（2026-07-13，同台 P40 Pro 电信 5G SA，run 019f5af6）**：硬切换拆除原绑定蜂窝 net119 → `CONTINUITY_REBIND ok=true` → 新蜂窝默认网 net120 上线 → `CONTINUITY_RECOVERY semantic=cross_network recovery_ms=7737ms` → `status=completed`（原缺陷同场景为 EPERM×5/recovery_failed），DB `c2CrossNetworkRecoveries=1`（v11 迁移端到端）；重连错误由 `EPERM Binding socket to network`（死句柄）变为 `ConnectException`（在新默认网尝试），精确证明"不再在死句柄上重试"。本环境无可用 WiFi AP，以 `svc data disable→enable` 令蜂窝新句柄作新默认网、等价复现 §3（net110 死、新蜂窝网上线），代码路径与 蜂窝→WiFi 完全一致；真正 蜂窝→WiFi 变体待有 WiFi AP 会话补测 | evidence/phase3/realdevice_continuity_crossnet_fix_verify_20260713.md（修复验证）；evidence/phase3/realdevice_continuity_kimi_20260713.log §3（EPERM 现象）；evidence/phase2/continuity_e2e_20260713.log（模拟器 508ms 基线，transport=auto）；D-20（C2 口径）；KPI 文档 §5.1 |

| D-24 | 2026-07-14 | **VpnService 流量观测模式（设计定案，本轮只出文档、不实施）**：为填补"手机端第三方**封闭** AI App（豆包/Kimi 客户端等）真实会话网络体验"这一测法空白——此类 App 无公开流式 API（ProviderPresets excludedNote 明确排除飞书 Aily/蚂蚁阿福），API 探针测的是"我用官方 API"而非"用户用官方 App"、干净路径仿真更测不到真实 App 加密流。这是三种 App 测法里最重最敏感的一种，故先设计清楚再决定建不建。决策：①**确立为独立互斥模式**：ANEB 作本地 VpnService 建 TUN 虚接口 → 读 IP 包 → 按用户显式选定 App 的 UID 过滤（PackageManager/ConnectivityManager 取 UID，per-UID 路由）→ 用户态 TCP/UDP 协议栈把流量重新转发出去（TUN 只给 IP 层，需自建流重组+转发或用成熟 tun2socks 思路）→ 逐包时序记录——**从加密流的时序推断，不解密内容**；这是**全项目工程量最大的一块**（TUN + 包解析 + 流重组 + 转发），量级远超现有 OkHttp/SSE 测量栈。②**能测 vs 不能测边界钉死、全部标"流级推断，近似"**：能——SYN→SYN-ACK 连接建立 RTT、TLS 握手成功/失败（ClientHello→是否被 RST，即 SNI-RST 类，与 R-33/D-22 同源、复用同一 ClientHello 观测面）、SNI 主机名（ClientHello 明文）、DNS 查询时延、上行请求突发字节量与时刻、**下行响应字节到达节奏**（请求突发→首个响应包≈TTFT 代理；下行包间隔停顿≈ITL/stall 代理）、字节总量；不能——解密内容、精确 token 边界（TLS record 合帧/分片使"逐 token"只能是**流级近似**、非应用层精确）、区分 token 与控制帧。③**新 claim_scope=`application_flow_observation_no_decrypt`**，与 `application_end_to_end_to_probe_node`（仿真节点）、`application_end_to_end_to_llm_api`（LLM API 探针）口径明确分开，结论一律标"流级推断"、**不进 AQS**。④**隐私与同意模型**：只观测本机 + 用户显式选定 App + 不解密 + 不留存 payload（只留元数据时序）+ 数据只存本机（呼应 §9.1 GPS 只存本机）；VpnService 系统强制显示持续通知与 VPN 图标（Android 不可关）；首次需明确的知情同意界面（说明抓什么/不抓什么/仅观测期）；随时可停。⑤**与 D-16（本机测量代理红线）/R-03（NetGuard VPN 硬拒测）的关系**：ANEB-as-VPN 是**独立模式**，与 ANEB 干净路径测量（仿真节点/API 探针）**互斥**——VPN 开启期间干净路径测量必须禁用（VPN 自身用户态转发会污染路径、批化，正是 R-03/D-16 判为"不可作运营商中间盒证据"的场景），反之干净路径测量期间 VPN 观测必须关；明确模式切换状态机。⑥**正当性边界**：自有设备 + 用户自选 App + 不解密 + 只存本机的**授权网络性能观测**；**非中间人、非抓他人流量、非规避**；明确不做——不解密 TLS、不注入证书、不 MITM。⑦**分期建议（本轮不实施）**：推荐先出本设计文档、暂不建；若将来建，MVP 只做**连接层确定可靠量**（SYN-RTT + TLS 握手成功率/SNI-RST + DNS 时延 + 字节量，这些无需解密即确定可靠、且与 REACH/R-33 口径自然衔接）；**下行节奏→TTFT/ITL 推断作二期**（流级近似度需先用 API 探针/仿真节点已知真值交叉验证后再启用）；给出可选最小 spike（若将来验证可行性该做什么）但本轮不做。**用户裁定（2026-07-14）：采纳推荐＝暂不建，VpnService 不实施；如将来重启，先做一期连接层确定量（SYN-RTT/TLS 握手/SNI-RST/DNS/字节量）再议二期下行节奏推断。item3 就此收口。** 设计详见设计文档 §11 | 设计文档 §11（新增，设计未实施）；ProviderPresets.kt excludedNote（封闭 App 测法空白 + "走 REACH 或 VPN 流量观测"指引）；R-33 / D-22（SNI-RST 同源、VPN 观测复用同一 ClientHello/握手观测面）；D-16 / R-03（VPN 用户态转发污染路径→互斥模式边界的直接依据）；§9.1（"只存本机"隐私锚）；§4 测量方法学（流级近似 vs 应用层精确的口径分界） |
| D-25 | 2026-07-14 | **SNI-RST 自动旁路：测量端点按 REACH 结果选路（修复真机蜂窝"未完成无结果"）**。真机 P40 电信 5G SA 实测暴露：app 默认端点是 E-01 sslip 主机名 `https://120-79-148-0.sslip.io:8443`，电信蜂窝路径 DPI 按 SNI 注入 TLS RST（R-33/D-22 同源）；run 前 REACH 探针虽已探明 `sni=rst / ip=ok`，但**测量仍死磕被 RST 的 SNI 路 → 每场景 Connection reset → 全 SCENARIO INVALID → AQS=null（用户所见"未完成无结果"）**。根因＝REACH 结果此前只用于记日志/落库，未回作用到测量选路。修复：新增纯函数 `ReachabilityProbe.preferredMeasureBase(base, reach)`——仅当"配置端点是 E-01 sslip 主机名 + SNI 被 RST + bare-IP 可达"时返回 bare-IP 等价基址，其余情形（未探测/非 E-01/SNI 本就通/已在 bare-IP）一律不变；TestEngine run 前算 `measureBase` 用于 profiles 拉取/场景测量/结果上报/落库，切换时打 `REACH_SWITCH` 日志。**同一节点、同一物理路径，仅换 SNI/证书绕过 DPI 的 SNI-keyed RST，claim_scope（application_end_to_end_to_probe_node）不变、无测量偏差**；SNI 本就可达时保留观测真实 sslip 路径（不掩盖 SNI-RST 研究信号，REACH 字段照记 sni 状态）。debug 版已内置 aneb_ip_ca 信任锚故 bare-IP 的 IP-SAN 证书可信（release 版另需配锚）。真机实证：默认 sslip URL → 全 Connection reset/AQS=null；手动 bare-IP URL → 场景 VALID_LOW_CONFIDENCE 真实 KPI——本修复即把该手动旁路**自动化**。7 项选路单测锚定；346 单测全绿。 | ReachabilityProbe.preferredMeasureBase + ReachabilityBaseSelectTest；TestEngine（measureBase 选路 + REACH_SWITCH）；R-33/D-22（SNI-RST 同源、bare-IP+IP-SAN 旁路）；deriveE01Pair（sni/ip 基址对） |
| D-26 | 2026-07-15 | **R-15 的 `nr_state` 停止读取 Android 私有 API**：删除 `ServiceState#getNrState` 反射与 `toString()` 内部格式解析，字段名和日志键保持不变，改为仅用公开三元组保守推导。只有 `dataNetworkType=NR` 且注册小区为 NR 才记 `connected`；一致 LTE 且 display override 明确回报 `none` 才记 `none`；图标单独声称 5G、来源冲突、回调尚未报告或 API 不可用一律记 `nsa_unknown`。不改变 KPI/AQS；R 组继续只作归因协变量。 | Product Owner 2026-07-15 批准推荐方案；R-15；targetSdk 35 `BlockedPrivateApi`；`PublicNrEvidenceTest` |
| D-27 | 2026-07-15 | **V1 主测量改由 `dataSync` 前台 Service 持有**：`ProbeRunService` 拥有 `TestEngine`、run 协程和持续通知；Activity 只订阅 session/logs/telemetry StateFlow。切后台或 Activity 配置重建不再取消 run，通知可取消，回到 App 可恢复测试中/结果/失败状态。ADB autorun 仍走同一引擎与原日志 KEY；KPI、AQS、场景顺序、节点选路和数据 schema 不变。Continuity/AB 专项暂保留原自动化入口，后续 C 阶段单独迁移，禁止假称已具备同等后台保证。 | Product Owner 已批准按推荐方案推进；V1_SCOPE background/resume；Android FGS dataSync |
| D-28 | 2026-07-15 | **APK 视觉以 `E:\G Project\ANEB\ANEB_UI` 的 `2026.07.15-2` 交付为唯一权威来源**：22 个交付文件与工程内 `design_handoff_aneb_probe` SHA-256 一致；采用原生 Compose 复刻深海军蓝/青色视觉、五栏导航、270° 仪表、地图壳、探针/历史/设置/分享结构，并重做自适应矢量 App 图标。HTML 中的演示数字、虚构城市热区和虚构 API 健康状态不得进入 APK；真实数据缺失时显示 `—` 或明确空状态。视觉一致性不能凌驾于 D-02/D-12 的证据真实性。 | Product Owner 要求 APK 严格按 `ANEB_UI` 更新并同时重设计 App 图标；UI manifest 与真机对照；禁止假数据 |
| D-29 | 2026-07-15 | **Token 模式的唯一实时主指标改为“AI 流式到达速率”**：SSE 读线程只在完整 event 边界做常数时间到达戳记录，独立遥测协程计算最近 1 秒 event/s，UI 每 250ms 用弹簧动画驱动 270° 指针、数字与曲线。该值明确标为仿真流式事件代理量，不等于有效模型 token 或计费 usage；EOF 后的 KPI/AQS/解析/落库口径不变。缺样本时不以 0 驱动指针，改显“建立速率窗口”。 | Product Owner 要求测试指标实时波动且达到 SpeedTest 级动态效果；现状根因是 token 累计值只在场景结束后更新；R-10/R-16 |
| D-30 | 2026-07-15 | **ANEB 采用“独立模式引擎 + 共用 Profile 外壳”扩展测试能力**：首批 `network_basic` 与 `token_experience`。Profile 必须声明 mode、业务说明、phase、指标清单、实时主指标、窗口/刷新频率和 `conclusion_policy_id`；结论公式由版本化客户端实现，不允许节点下发任意脚本。基本模式测下载/上传/RTT/抖动/应用层请求失败率，不与 AQS 混分；Token 模式保留现有三场景与 AQS。两者 claim scope 均限定到指定端点的应用层路径。详见 `TEST_MODE_PROFILE_DESIGN_2026-07-15.md`。 | Product Owner 要求支持与 SpeedTest 同项目的基本网络测速、Token 体验模式，并能通过新增 Profile 持续扩展；避免巨型统一引擎污染口径 |
| D-31 | 2026-07-15 | **基本测速结论采用版本化产品策略 `network-basic-conclusions-v1`**：结果页把测量事实与产品判断分开，并为 4K 视频、视频会议、大文件上传逐项给出“适合/受限/证据不足”。V1 条件：4K＝下载≥25 Mbps 且应用层请求失败率≤2%；视频会议＝上下行≥5 Mbps、RTT≤100 ms、抖动≤30 ms、应用层请求失败率≤2%；大文件上传＝上行≥10 Mbps。上述是 ANEB 产品阈值，不宣称为运营商 SLA/行业标准；缺指标一律“证据不足”，不得按 0 判差；调整阈值必须升 policy 版本。 | Product Owner 要求每个测试场景给出可行动结论；R-10 null 语义；避免无版本的展示层暗改判断 |
| D-32 | 2026-07-16 | **Profile 目录先做合同审计，不擅自改变 AQS 编排**：设置页新增 Profile Registry，优先展示当前节点下发的 Profile，拉取或解析失败时明确标注 APK 内置兜底；逐项展示业务、版本、实时主指标/窗口/刷新频率、输出指标、结论策略、phase 与当前引擎兼容状态。新增 `ProfileCapability` 对未知 mode/phase 和缺失展示合同标“需要引擎适配”，禁止静默宣称可执行。Token 模式仍按 S1/S2/S3 固定输入映射生成 AQS，基本测速仍独立运行；单 Profile 专项运行会改变 §5.3.6 编排与 AQS 完整性，未经单独裁定不实施。 | Product Owner 要求 Profile 可持续扩展；D-30；AGENTS 编排变更红线；本轮推荐方案先完成可审计目录 |
| D-33 | 2026-07-16 | **C 阶段生产化收口，不改变测量算法**：① Continuity/Protocol A/B 的协程所有权从 Activity 迁到独立 `dataSync` 前台服务，Runner、参数、日志 KEY、KPI/AQS 均不变，解决配置重建/切后台取消；② release 版本升为 `0.2.0`/20，Room v12 schema 导出入库；③ E-01 bare-IP IP-SAN CA 公钥进入 main Network Security Config，全局仍禁明文，补齐 D-25 在 release 的自动旁路；debug 继续仅对白名单联调地址允许明文；④ release assemble/bundle/install 增签名 fail-closed 门禁，密钥由 Product Owner 持有且不入库；⑤ Keystore 不可用时 API key 从“私有明文降级”改为禁止保存，并清除旧 fallback，非敏感 provider/base/model 另存私有配置。 | Product Owner 要求按推荐方案完成 A→B→C 与公开发布准备；D-25/D-27；V1_SCOPE background/resume、privacy-safe evidence、release signing readiness |
| D-34 | 2026-07-16 | **构建工具升级但 targetSdk 暂保持 35**：AGP 8.5.2 官方只验证到 API 34，故升级到 AGP 8.13.0 + Gradle 8.13（wrapper SHA-256 钉死），该组合官方支持 API 36.1；debug/release 双变体编译通过。`compileSdk/targetSdk` 仍为 35：截至 2026-07-16 Google Play 官方最低要求是 35，直接切 36 会启用 Android 16 定向行为，必须先做 Android 16 设备/模拟器专项回归，不能只为清除 `OldTargetApi` 警告盲升。Kotlin 2.0.21 的升级提示亦留到独立依赖批次，避免与本次产品/数据库/后台变更耦合。 | Android 官方 Play target API 要求、Android 16 setup、AGP 8.13 compatibility；供应链精确版本纪律；双变体构建证据 |
| D-35 | 2026-07-16 | **Profile 目录的节点读取复用 D-25 双通道选路，但不改变测量编排**：P40 真机验收发现测速主引擎可按 `sni=rst / ip=ok` 自动旁路到 E-01 bare-IP，而 Profile Registry 仍直接访问默认 SNI 地址，导致目录误显示“APK 内置兜底”。目录读取现先执行同一 REACH 双探针，仅在 D-25 的严格条件成立时用 bare-IP 读取节点合同，并记录 `PROFILE_CATALOG_ROUTE`；非 E-01、SNI 正常、bare-IP 不可达或未探测时均不切。该路径只决定配置展示来源，不改变 Profile 内容、测试阶段、AQS、结论策略或结果上报。真机复验显示“节点已核验 / 4 个 Profile”。 | P40 Pro 真机截图 `evidence/ui/aneb_codex_c_profiles_server.png`；日志 `PROFILE_CATALOG_ROUTE route=bare_ip reason=sni_rst_ip_ok`；D-25/D-32 |
| D-36 | 2026-07-16 | **ANEB App 的正式测试只执行自建节点可控仿真，第三方 AI 真实 API 不进入 Profile、主测量或评分；AI 真实业务行为建模拆为独立工程并行推进。** App 负责按冻结 Profile 生成/接收确定性的上传、处理阶段、Token/音频事件并测量网络影响；独立 `aneb-ai-behavior-model` 负责从获准观测数据或显式假设拟合业务行为、生成版本化模型/金轨迹/候选 Profile。两者以模型 id、版本、哈希连接。Kimi/DeepSeek/千问仅可作业务原型标签；未经校准必须标 `hypothesis`，不得声称厂商真实性能。现有真实 API 探针不参与新 Profile 或评分，其正式产品入口去留另行裁定。 | Product Owner 明确“ANEB APP 是模拟 Kimi 等 AI 应用行为，不是真实 API；另做模型和算法模拟真实业务行为，与 App 分开并行”；`PROFILE_CONTRACT_V2_PROPOSAL_2026-07-16.md` |
| D-37 | 2026-07-16 | **批准 Profile Contract v2 的 provisional v1 质量目标、95% 达标口径、三套独立评分权重与门控作为实验性实施基线；批准真实 API 探针退出正式产品入口。** ①Token、AI 实时交互、网络综合性能各自独立出分，禁止与既有 AQS 或彼此混分；目标来源固定标 `aneb_product_provisional_v1`，后续改数值必须升 target/score policy 版本并追加决策；②必需指标缺失不重分权、分数为 null，invalid 保留原始证据但抑制聚合；③100MiB 视频使用独立 stress Profile；④正式 App 入口只展示自建节点仿真，现有真实 API 探针代码保留为开发诊断能力但不在正式导航/设置中出现、不进入 Profile 或评分。 | Product Owner 回复“按推荐方案”；D-36；`PROFILE_CONTRACT_V2_PROPOSAL_2026-07-16.md` |
| D-38 | 2026-07-16 | **将会话 `019f4b68-e951-78b0-a670-2f61c8748352` 的智能体网络诉求纳入 ANEB 测量架构，但不静默覆盖 D-37 门限。** 立即采用：五类业务原型路线图、应用 RTT/截止时间可靠性/有效丢失/负载时延增量/逻辑会话连续性/Trace ID 的口径，以及 `PASS/FAIL/INCONCLUSIVE` 三态证据规则。参考会话中的更严格数值冻结为 `agent-network-enhanced-targets-v0-draft` 候选集；只有 Product Owner 批准并升 target/score policy 版本后才能进入正式评分。 | Product Owner 要求参考该会话结论优化 ANEB；`AGENT_NETWORK_REQUIREMENTS_MAPPING_2026-07-16.md`；D-12/D-36/D-37 |
| D-39 | 2026-07-16 | **Token Simulation Score v1 使用版本化 `compliance-anchors-v1`：先按原始样本计算业务门限达标比例，再以 `(100%,100)、(95%,85)、(90%,70)、(80%,55)、(0%,0)` 分段线性计分。** 该口径直接支持“门限达到 95% 比例”的产品结论；任务成功、流完整、准时 Token 等比例指标直接使用其比例，时延/速率类按逐样本是否过门限计算。严重卡顿 `TOK-B10` 只作门控，不重复加权。上传期限使用 `token-upload-deadline-v1`：文本 1s；文档 `max(2s, 1s+1s/MiB)`（5MiB=6s）；图片 `max(2s,1s/MiB)`（10MiB=10s）；100MiB 视频 60s 且仅在独立 stress Profile。快测样本不足时最高只能给 medium/low 置信度；不能用快测冒充 standard 高置信结论。 | D-37 已批准五锚点评分框架、Token 组权重和门控；`PROFILE_CONTRACT_V2_PROPOSAL_2026-07-16.md` §4.5/§7；实现推荐方案 A |
| D-40 | 2026-07-16 | **Token 仿真的可执行边界冻结为“模型产出哈希绑定的业务计划，App 在真实路径执行，节点只按计划回放”。** Quick/Standard 均发布 `profile.json + runtime_plan.json + manifest.sha256`；App 必须校验 Profile、模型与运行计划哈希后才可执行，并通过一次真实上传请求接收逐 Token SSE。计划和节点协议禁止携带 RTT、丢包、抖动、速率等网络结果字段；这些结果只能由真实路径测量产生。Token 结果使用 Room 独立表和独立评分，不能混入旧 AQS。Quick 只作功能/方向性诊断并强制 LOW/INCONCLUSIVE；Standard 达到全部最小样本数后才允许 HIGH 置信。 | D-36～D-39；P40 Pro 两次 Quick 端到端验收；`aneb-token-runtime-plan-v1` / `aneb-token-task-v1` |
| D-41 | 2026-07-16 | **AI 实时交互仿真采用固定 20ms 双向音频帧和独立 WebSocket 合同；动态主指标冻结为 2 秒窗口内的 150ms 限时送达率。** 行为模型只发布哈希绑定的会话、轮次、帧大小、处理等待与打断计划，不注入 RTT、抖动、丢包或速率结果；App 通过应用 ping/pong、服务端绝对调度时戳、客户端单调到达时戳测量 RTT、到达变动、准时帧、卡顿、补偿、打断和连续性。评分独立使用 `realtime-interaction-score-v1`，分为对话响应 35%、播放连续性 35%、会话可靠性 20%、网络准备度 10%；轮次成功率低于 80% 或卡顿帧率高于 5% 时总分封顶 54。Quick 为 1 会话/最多 3 轮，强制 LOW/INCONCLUSIVE；Standard 为 10 会话，只有全部最小样本数满足时才允许 HIGH。结果冻结到 Room v14，历史页不得按新算法重算。D-38 enhanced 数值仍是候选，不静默替换 D-37 provisional v1。 | D-37～D-40；`aneb-realtime-runtime-plan-v1` / `aneb-realtime-session-v1`；会话 `019f4b68-e951-78b0-a670-2f61c8748352` |
| D-42 | 2026-07-16 | **网络综合性能从基础测速升级为可执行 Profile v2，固定动态主指标为负载 RTT，吞吐只作为阶段辅指标。** 发布 Quick/Standard 1.0.0，阶段固定为握手、空闲 RTT、下载+并发 loaded RTT、上传+并发 loaded RTT、带序号 UDP 应用探针和测后恢复 RTT。评分独立使用 `network-comprehensive-score-v1`：响应性 30%、容量 35%、稳定性 35%；Quick 强制 LOW/INCONCLUSIVE，必需指标缺失时总分为 `null` 且不重分配权重。UDP 探针采用 `ANEB1 + seq + monotonic timestamp`，与 HTTP/3 共用 E-01 UDP/8443 并按魔数分流；零回包或不可达只能写“UDP 应用探针不可用”，不得宣称精确 IP 丢包率。结果及原始证据冻结到 Room v15，历史页不得按新算法重算。D-38 enhanced 数值继续保持候选，不替换 D-37 provisional v1。 | D-37～D-41；会话 `019f4b68-e951-78b0-a670-2f61c8748352`；P40 Pro runs `019f6b5f-60f4-7103-ab40-bee002d9effd` / `019f6b6f-d3d8-7063-b301-90ec8be6fa5e` |
| D-43 | 2026-07-16 | **100MiB Token 大对象测试作为独立 Stress Profile 发布，绝不混入 Standard。** Profile 固定为一次 100MiB 视频突发上行、300 个仿真 Token 和一次 100MiB 大对象下行；App 在上传/下载同时并发 echo，实时展示阶段 Mbps、负载 RTT 与 Token 到达。独立 `token-stress-score-v1` 权重为任务完成 20%、上行容量 30%、下行容量 25%、负载响应性 25%，必需指标为 TOK-B01/B02/B11/N05/N06/N07/N08/N09；任务或 Token 流不完整时 FAIL 且封顶 54，必需指标不足时分数 null。单次 Stress 即使成功也固定 `LOW/INCONCLUSIVE`，不得形成 95% 长期稳定性承诺。E-01 单任务上传硬上限从 16MiB 提升到受控 128MiB 并升级 `aneb-server/0.5.0`；App 启动前明确提示约 200MiB 流量/发热。P40 Pro run `019f6b94-e09a-770a-83a2-6cb5dc6ee38d` 完成 100MiB/100MiB 与 300/300 Token，采集 95 个 loaded RTT 样本，结果 50.5/D、LOW/INCONCLUSIVE；上行 29.9Mbps 达标，下行 18.2Mbps 与 loaded RTT P95 1026.7ms 未达建议目标。 | D-37/D-39/D-40；`token_multimodal_stress@1.0.0`；P40 原始 Room 证据与 `evidence/ui/aneb_stress_*` |
| D-44 | 2026-07-17 | **AI 实时结果必须覆盖 Profile 声明的全部 21 项指标，并把“未触发”与“测得 0”分开。** 13 个必需指标继续按 `realtime-interaction-score-v1` 出分；原始轮次响应、最大连续未返回帧、双向 P05 净荷速率、通话 loaded RTT、非计划重叠、重连/恢复和无线协变量作为完整结果冻结。loaded RTT 在实际语音上下行期间每 250ms 并发 echo；恢复终点固定为自然中断后下一成功会话的首个有效音频帧。没有中断时恢复时延为 null/0 样本，重连事件可合法为 0；Standard 未满足全部最低样本量时不得 PASS。LIVE-N03 严格按 P95−P50 实现。LIVE-N06 的 provisional v1 `双向≥0.5Mbps` 与模型净荷基线 0.256/0.384Mbps 冲突，本轮不静默改目标且该项不入分，后续须升 target-set 版本裁定。P40 run `019f6bbd-e628-76bb-b88e-26edf9f502b8` 冻结 21 项与 79 次 loaded RTT：会话/负载 RTT P95 42.8/54.2ms，上/下行 P05 0.257/0.383Mbps，100/A 但按 Quick 保持 LOW/INCONCLUSIVE。 | D-37/D-41；`ai_realtime_voice_quick@1.0.0`；P40 Room 原始证据与 `evidence/ui/aneb_realtime_fullmetrics_*` |
| D-45 | 2026-07-17 | **AI 实时受控恢复必须使用独立 Profile 和独立 Score v2，禁止把故障注入 Standard，也禁止用故障前好样本掩盖恢复后质量。** `ai_realtime_voice_recovery@1.2.0` 固定 4 会话、2 次连接级受控服务端中断；节点先完成指定轮次再只关闭该 WebSocket。两个恢复会话首轮复用同一模型派生刺激（1.2s 语音 + 350ms 模型等待），恢复时延仍按“客户端检出→首个有效音频帧”冻结，并披露 1550ms 业务基线。`realtime-recovery-score-v2` 的必需指标 LIVE-B05/B09/N02 只取恢复后会话，最低覆盖 400 帧/6 轮/10 RTT；LIVE-B11 覆盖 2 次预期恢复，失败尝试按未达标计入。v1 首轮真机曾因不同输入长度得 78.1/B、HIGH/FAIL；v1.1 又暴露全 run 99.09% 掩盖恢复后 98.72% 的假 PASS，因此两版均由新证据否决并升 v2。最终 P40 run `019f6df2-adf6-7e63-bf4d-7db123d8e58a`：2/2 中断观察并恢复，2468.4/2814.1ms、P95 2796.8ms，恢复后 467/467 帧、6/6 轮、10/10 RTT 达标，100/A、HIGH/PASS。结论只适用于受控服务端中断，不外推到蜂窝断网、跨网迁移或第三方 AI 服务。 | D-41/D-44；`aneb-server/0.5.1`；P40 Room 原始证据 `evidence/device/aneb_realtime_recovery_0.4.4.db*` |
| D-46 | 2026-07-17 | **AI 实时 Standard 的 PASS/FAIL 必须服从逐指标质量门限，综合分和等级不能覆盖任一必需指标失败；结论首屏必须点名失败指标及差距。** P40 Pro Wi-Fi run `019f6e12-b462-7cd9-9ff7-713e8a6c6df7` 完成 10/10 会话、160/160 轮、26518 帧，取得 92.1/A、HIGH，但 LIVE-B04 响应超额时延 P95 247.7ms，≤200ms 达标率仅 85.625%（要求 ≥95%），因此 `realtime-interaction-score-v1` 正确判 FAIL；音频准时帧率 99.68%、会话 RTT P95 71.6ms、loaded RTT P95 89.2ms 均达标。0.4.5 结论增加全部未达必需门限的 `{metric id, 名称, 实际达标率, 目标率}`，并明确解释“高分/高等级仍 FAIL”。该 run 承载为 Huawei-Guest Wi-Fi，不外推到蜂窝；首次强制蜂窝尝试因系统无已连接蜂窝句柄正确 INVALID。ADB autorun 测试退出时必须同时强停包并移除最近任务，避免任务恢复后重复消费旧 Intent。 | D-41/D-44；P40 Room 原始证据 `evidence/device/aneb_realtime_standard_0.4.4.db*`；App 0.4.5 |
| D-47 | 2026-07-17 | **E-01 唯一部署权归 Codex；服务器能力以仓库 `docs/TEST_SERVER_CAPABILITIES.md` 为权威，任何二进制/Profile/端口/限额变化必须同提交更新并在部署后回写。** 按 Claude 需求在现役 `aneb-server/0.5.1 + s3_multimodal@0.2.1` 上只合并两段 `download_burst`：每段 12MiB/256KiB，分别紧跟两段 Token 流，Profile 升为 0.3.0/82s/10 阶段。服务端解析无 phase 白名单；部署脚本新增 exact contract fail-closed smoke。Codex App 0.4.6 同步实现该 phase，只有 2xx、响应体排空且字节数精确匹配才向机器日志输出原始 D1，否则 null；旧版 Room/token-experience AQS 不加字段、不变分。12:03 CST 节点本机 Profile/echo/download/UDP 与公网 Profile/12MiB 字节回归通过。弱网不得在共享主机使用全局 netem：软件损伤只能逐 run/逐连接隔离并标 `synthetic_impairment`；真实 RSRP/SINR 只能由射频环境产生。 | `SERVER_UPDATE_NOTES_FOR_CODEX_2026-07-17.md`；`profiles/s3_multimodal.json`；公网 E-01 smoke；`docs/WEAK_NETWORK_TEST_PLAN_2026-07-17.md` |

| D-48 | 2026-07-17 | **首个弱网能力采用 E-01 用户态逐 run 聚合整形，禁止修改真实无线指标或共享主机全局网络。** `aneb-server/0.6.0` 仅在 `/synthetic/weak-capacity-latency-v1` 对 echo/download/upload 套用聚合下行 3Mbps、上行 1Mbps、请求附加 RTT 120±30ms；并发连接共享 run 限速器，不同 run 独立，正常路由旁路。DNS/TCP/TLS/UDP/RSRP/SINR 明确排除，初版不模拟 IP 丢包或断线。App 0.4.7 必须核验服务器回执，Room v16 冻结参数/排除项/确认状态；弱网上行只按服务端确认字节计量，拒绝把本机 socket 写入量当线上 goodput。P40 相邻对照：Standard `019f6e96…` vs 弱网 `019f6e93…`，下载 P5 17.66→2.80Mbps、上传 P5 15.91→1.12Mbps、空闲 RTT P95 109.80→228.03ms、分数 51.2→32.0；loaded RTT 非单调，故结论只限容量/应用时延敏感性，不外推真实无线弱覆盖。`weak_recovery` 仍是待开发 Profile。 | `network_comprehensive_weak_capacity_latency@1.0.0`；`docs/TEST_SERVER_CAPABILITIES.md`；`docs/WEAK_NETWORK_TEST_PLAN_2026-07-17.md`；P40 Room v16 原始结果 |

| D-49 | 2026-07-17 | **受控请求中断恢复必须是独立 Profile、独立路由与独立评分，不与无故障 Standard 或容量/时延弱网混分。** `aneb-server/0.7.0` 新增 `network_comprehensive_weak_recovery@1.0.0` / `weak-recovery-v1`：每个 run 仅能武装一次 2000ms 应用请求不可用窗口；窗口内只有同 run 请求返回带服务端确认头的 503，其他 run 与正常路由保持 200，窗口后自动恢复。合成基线为 ↓5/↑2Mbps、附加 RTT 80±20ms；DNS/TCP/TLS/UDP/IP 丢包/切网/RSRP/SINR 均排除。恢复时间固定为 trigger 202 回执到首个成功 echo，独立 `network-recovery-score-v1` 按恢复 45%、连续性 30%、响应性 25% 评分；中断未观察、恢复失败或必需样本缺失时硬失败/抑制分数；单次事件即使达标仍固定 LOW/INCONCLUSIVE。App 0.4.8/Room v17 增加 100ms 动态恢复仪表、回执核验与恢复证据字段。P40 4 次独立 run 恢复 2084.4–2227.3ms、中断失败 8–9 次、恢复后成功率均 100%，末次 RTT P95 152.6ms；动态序列捕获 1063.2ms/5 次失败/中断指针。结论只适用于 E-01 声明的应用请求中断，不外推真实蜂窝断网。 | `network_comprehensive_weak_recovery@1.0.0`；`docs/P40_WEAK_RECOVERY_VALIDATION_2026-07-17.md`；Room v17 原始结果 |

| D-50 | 2026-07-17 | **网络层弱网必须由独占转发网关实施，禁止在 E-01、开发 PC 或共享路由接口挂全局 qdisc。** `aneb-gateway/0.1.0` 只执行版本化白名单，以 WAN egress + WAN ingress→IFB 分别控制上/下行；TLS+Bearer 管理面留在未整形 LAN，强制专用设备证明、单实验、120s 上限、状态审计与启动/结束/信号/systemd 多重清理。App 0.4.9 Debug 以 Profile 指纹、run id、claim scope、激活/清理回执和 active 期旁路探测形成 fail-closed 证据；Room v18 冻结。命名空间实测 0.061→105.260→0.054ms、100% 中断恢复与宿主 qdisc 不变均通过；因尚无独占双网口 Linux/AP，P40 网络层项必须保持 BLOCKED_EXTERNAL。该能力不改变 RSRP/RSRQ/SINR、基站调度或真实切网。 | `gateway/`；`network_comprehensive_gateway_loss@1.0.0`；`network_comprehensive_gateway_recovery@1.0.0`；`docs/DEDICATED_GATEWAY_PLAN_AND_VALIDATION_2026-07-17.md` |

| D-51 | 2026-07-17 | **专用网关必须证明“失败时没有伤害”，不能只证明正常路径能限速。** `aneb-gateway/0.2.0` 把静态自声明升级为每次启动的实时双网口/管理 IP/客户端子网/路由/转发验证；证书、Token、Profile、拓扑和监听端口全部通过后才允许网络操作。WAN 变更前持久化安全基线和所有权，清理只接受固定 handle/filter/IFB alias；未知资源拒绝触碰，基线不一致进入 `cleanup_failed` 锁闩、health 503 且不写 `cleared_at`。systemd 停止复用同一 `-cleanup-only` 验证器；安装器提供全局锁、原子 staging、TLS health、失败回滚和默认保留日志的安全卸载。App 0.5.0 在 POST 回执后立即登记 experiment id，回包不确定时仅按同 run `/v1/status` 对账；Token 用随机句柄的一次性内存凭据交接；失败/取消先清理再冻结证据。网关恢复计时诚实改为“APP active 回执→首个 echo 完成”，Profile 升 `1.1.0` / score v2；active 期旁路或未观察到任何中断失败均 `INVALID`。隔离复验 `0.067→97.753→0.051ms`、10 条同步操作事件、缺失私钥前置失败零网络变更、100% 中断恢复与 E-01 默认 qdisc 不变通过；P40 硬件项仍为 `BLOCKED_EXTERNAL`。 | `gateway/README.md`；`gateway/scripts/install_gateway.sh`；`network_comprehensive_gateway_recovery@1.1.0`；`docs/DEDICATED_GATEWAY_PLAN_AND_VALIDATION_2026-07-17.md` |

| D-52 | 2026-07-17 | **专用网关发布必须绑定固定 Debug CA、全路由拓扑和可恢复清理；早期数据面集成不能冒充最终发布证据。** 发布复审关闭 9 项 P1：IFB 类型/alias 与 ingress/filter 精确归属、删除失败后可再次 Clear、第三出口/隧道/目标路由旁路拒绝、真实转发与回程、固定 CA 每次 Go 启动重验、状态目录 symlink/mount/元数据保护、Token/TLS key 安全来源与旧 Token 不复用，以及 App 启停不确定回包的有界对账/幂等重试。明确 409/423/其他 4xx 必须立即失败且不查询状态、不重发 POST；只有连接中断或 202 响应无法解析才视为提交结果未知。Release APK 不含网关 CA 且拒绝控制；Debug App 与网关固定 CA DER SHA-256 为 `2089A92C…D361`。Go 定向回归重复 20 轮、E-01 `/tmp` Linux root 安装安全回归和全量质量门通过，E-01 保持 0.7.0/active、网关 inactive、默认 `fq_codel`、无命名空间。`0.067→97.753→0.051ms` 仅作为加固前数据面证据；仓库不持有 CA 私钥或现场叶证书，且无独占双网口/AP，最终 TLS 正向生命周期与 P40 网络层真机均为 `BLOCKED_EXTERNAL`。 | `gateway/trust/aneb_gateway_ca.pem`；`gateway/scripts/install_security_test.sh`；`gateway/topology.go`；`gateway/tc.go`；`docs/DEDICATED_GATEWAY_PLAN_AND_VALIDATION_2026-07-17.md` |

| D-53 | 2026-07-17 | **ANEB 后续统一按“3 个子项目 + 1 个横切机制”管理，但现阶段保持单仓库内的逻辑边界，不做物理拆仓。** 子项目 1 是 Android ANEB App，子项目 2 是测试服务器/专用弱网网关，子项目 3 是独立的真实 AI 业务行为模型与校准工具；Profile 是贯穿三者的版本化契约与治理机制。Profile 的业务参数变化优先通过数据版本演进，只有新增测量原语或跨端契约时才改代码；JSON/YAML 只是载体，不作为架构目标。当前真实缺口不是目录名称，而是两套 Profile 契约尚未统一、结果 Schema 尚未冻结、M2 里程碑治理尚未启动，以及子项目 3 尚无获授权真实数据校准。 | 用户《ANEB系统开发计划_v1.0》；Claude《PLAN_ALIGNMENT_2026-07-17》；`docs/PLAN_ALIGNMENT_2026-07-17.md` |
| D-54 | 2026-07-17 | **正式 ANEB App 坚守“自建节点仿真”边界，真实付费 API 探针不得进入产品导航、Key 持久化或 Release 可调用组件；AI 实时测试的界面终态必须与持久结果一致。** App 0.5.1 删除正式 API Probe UI、KeyStore 和 MainActivity 自动入口；仅 Debug 变体保留受 `android.permission.DUMP` 保护、无 intent-filter、一次一任务且立即擦除 extras 的 ADB 诊断 Activity，Release 合并清单自动验收不含该组件。普通 autorun 只在 Debug 首次 Activity 创建读取并清除；手动权限动作/弹窗/事件由 `SavedStateHandle` 恢复且不持有 UI callback。AI 实时结果在进入最终提交前检查取消，随后以同一 `NonCancellable` 边界完成 Room insert + StateFlow publish；DB 失败不发布，取消碰撞时 durable result 优先决定 Completed。每会话 loaded RTT 监控在 `finally` 中回收；固定 Wi-Fi/蜂窝失效后只按同一承载重获，失败不降级。 | `docs/DEBUG_API_PROBE.md`；`scripts/verify_release_boundary.ps1`；App 0.5.1；501 JVM tests / 0 failures；Lint 0 errors；全量质量门 PASS |
| D-55 | 2026-07-17 | **M0 统一结果以 `aneb-result-v1` 为唯一语义合同；JSONL 只封装已冻结结果，导出器不得重算指标、分数、置信度、结论或 Profile 指纹。** `spec/catalog.json` 机器索引 3 个 Schema、2 个 Profile 家族、16 个 Profile、6 个哈希绑定运行包及 P1/P2/P3 消费边界；目录校验拒绝未索引资产、重复 key、非有限数、模型/seed/variant/hash 漂移。结果合同把 run 生命周期与评估 verdict 分离，缺失值必须 `null + explicit state`，invalid 保留原始证据但抑制评分，无线未采集必须写 `not_collected/permission_denied/unavailable`，禁止用 0 或虚构 RSRP/SINR。三类正式引擎均先持久化再发布；App 0.5.2 / Room v19 首先为 Token 新 run 在同一事务冻结类型化行与结果信封（Profile/运行计划哈希、App/设备/网络上下文、原始证据、评分审计字段、规范化 SHA-256），确定性 JSONL 核心只校验身份/摘要并原样输出。P40 run `019f70ed-ed0a-7897-b019-eff5a9a26dda` 已证明同 run 双行、Schema 零错误及摘要匹配。AI 实时/网络综合信封、正式三引擎 RadioCollector 和 UI 文件分享仍是后续接线，不计为完成。 | `spec/README.md`；`spec/schemas/aneb-result-v1.schema.json`；`scripts/verify_spec_catalog.py`；`scripts/verify_result_schema.py`；Room v19；Token result envelope tests；`docs/P40_APP_0.5.2_RESULT_V1_VALIDATION_2026-07-18.md` |
| D-56 | 2026-07-18 | **App 0.5.3 将 `aneb-result-v1` 纵向闭环扩展到 Token、AI 实时和网络综合三类正式引擎，并把单条冻结证据的 JSONL 保存/分享作为结果页正式能力。** AI 实时与网络综合在发布 StateFlow 前，以同一 Room 事务提交类型化行和结果信封；正常、guard/bind 失败与异常均保留可审计生命周期，网络取消另写 `cancelled` 信封，AI 实时取消继续服从既有“无最终结果则不发布”语义。Profile 声明但引擎未产生的指标必须显式 `missing`。AI 实时冻结 Profile 与 runtime plan 哈希；网络综合因 phases 内嵌，runtime artifact 明确 `not_applicable`，不伪造空 manifest。结果页保存/分享前重新校验 run/test/schema 身份和规范化摘要，失败即停止；旧记录无信封时禁用，不做历史补算。P40 runs `019f714a-b54f-787a-a992-2f0254417568` / `019f714b-ca9d-7aed-a669-533f4ff4a500` 均证明类型化行/信封一一对应、Draft 2020-12 零错误与摘要匹配；网络与 AI 实时 JSONL 均从系统下载目录读回校验，系统分享选择器实际拉起。三类正式引擎 RadioCollector 仍是独立缺口。 | App 0.5.3 / Room v19；514 JVM tests；`docs/P40_APP_0.5.3_RESULT_V1_VALIDATION_2026-07-18.md` |
| D-57 | 2026-07-18 | **App 0.5.5 把 1Hz Android 公开无线环境证据正式接入 Token、AI 实时和网络综合三引擎，并修复跨语言规范化摘要词法差异。** 每个 run 使用独立 Collector，在最终化时冻结；类型化结果、统一信封、`radio_sample` 与 `env_event` 同一 Room 事务提交。R01 以 `observed + null scalar + sample_count + radio-context evidence ref` 表达时间序列，不虚构单值；可分享信封移除经纬度。权限拒绝、不可用、未采集保持三种显式状态。0.5.4 真机审计发现 Kotlin 小数指数 `E-5` 与 Python 合同 `e-05` 导致 Token/AI 实时摘要不一致，因此 0.5.4 证据被否决；0.5.5 固定 Python 兼容数值词法并加入冻结向量。P40 最终三类 run 的 Schema 错误均为 0，Room/信封无线样本分别为 119/26/18，Python 独立摘要全部匹配；活动承载均为 Wi-Fi，无线信号只作环境协变量，Quick 结论保持 LOW/INCONCLUSIVE。 | App 0.5.5 / Room v19；522 JVM tests；`docs/P40_APP_0.5.5_RADIO_AND_CANONICAL_VALIDATION_2026-07-18.md` |
| D-58 | 2026-07-18 | **App 0.5.6 用冻结的同任务端到端 TTFT 证据关闭原计划 M1 的同点位重复性验收项，但不借此提升 Quick 单 run 置信度。** Token 原始任务新增稳定 `task_id`、节点单调时钟的 B03 处理时延和“节点完整收到上传→App 首 Token”的 B04 TTFT；B03/B04 为非必需评分指标，不改变既有必需指标权重、覆盖率、置信度或 verdict。重复性审计只接受 completed+valid、同 Schema/claim/Profile 指纹/运行包/App/设备/节点/承载/VPN、30 分钟内至少 5 个 run，并要求 B04 P95 与冻结原始任务一致、任务身份集合完全相同；按任务计算样本 CV，以任务 CV 中位数 ≤10% 判 PASS，最大 CV 只作诊断。P40 同 Wi-Fi 5-run/3-task cohort 的中位 CV 1.425%、最大 4.986%，Schema、摘要、类型化核心字段及 119×5 无线样本均闭环。各 Quick run 只有 3 个 B04 样本，仍为 LOW/INCONCLUSIVE；本证据不外推至蜂窝、真实厂商业务、跨设备或跨节点。 | App 0.5.6 / Room v19；525 JVM tests；`scripts/analyze_ttft_repeatability.py`；`docs/P40_APP_0.5.6_TTFT_REPEATABILITY_VALIDATION_2026-07-18.md` |
| D-59 | 2026-07-18 | **P3 v0.2.0 禁止“任意 JSONL 直接标 calibrated”，把授权、隐私最小化、训练/留出隔离和可复算发布证据设为硬门。** `aneb-token-observation-v1` 只允许派生 session 统计，未知字段拒绝；主体标识必须是数据集专用密钥的 HMAC-SHA256，普通 SHA256(account) 不接受。`aneb-calibration-dataset-v1` 要求真实授权用途、`content_retained=false`、observation/subject 两级训练留出零重叠与规范化摘要。`token-holdout-validation-v1` 每 workload 至少 training/holdout 20/10 个 session，payload/处理等待/输出 Token/Token 间隔 P50/P95 相对误差≤20%、pause 占比绝对误差≤0.05、转移矩阵逐行 TVD≤0.15。`promote-token` 与 validated build/runtime 发布均从原 manifest/holdout 现场复算，候选、报告或数据任一漂移即拒绝；calibrated 不能直接发布。仓库没有授权真实数据，4 个模型继续为 hypothesis，不能宣称厂商画像完成。 | behavior model 0.2.0；6 Schema catalog；31 tests；`docs/P3_CALIBRATION_PIPELINE_2026-07-18.md` |
| D-60 | 2026-07-18 | **M4 开测路径必须在产生流量前完成可自救校验，并对三类正式结果统一取得无线证据知情选择。** 手动开测先验证活动网络与节点根地址；正式包只允许 HTTPS，拒绝账号密码、query、fragment、API path 和非法端口，失败只在首页/设置页给可操作提示且不得启动 Service。Network/Token/AI 实时与兼容旧模式在无线证据不完整时都先解释电话/精确位置用途、不读取通话/联系人/IMSI，并允许授权、取消或低置信继续；通知拒绝仍不阻断测试。前台 Service 必须撑过 Home/回到 App，通知显示阶段和取消动作；用户取消后保留 cancelled/invalid 原始证据，但 score/grade 必须为 null。P40 已证明非法节点零服务、Network 模式用途说明、后台回到结果和取消评分抑制。 | App 0.5.7；531 JVM tests；`docs/P40_APP_0.5.7_NON_DEVELOPER_FLOW_VALIDATION_2026-07-18.md` |
| D-61 | 2026-07-18 | **设备端历史批量导出采用逐记录完整性 fail-closed，而不是“任一异常锁死全部”或“静默跳过”。** 每条冻结信封必须独立通过受支持的 `schema_version`、canonical digest、test type 与 run identity 校验后才可进入按时间排序的原样 JSONL；异常记录不得重算、修补或导出，但 UI 与机器日志必须分别报告拒绝数量和 run id。设备端“可验证”在此只指身份与摘要完整性，不能冒充对全部历史重新执行当前 JSON Schema。P40 22 条历史中 4 条 0.5.3/0.5.4 旧摘要异常被隔离，18 条完整性合格结果成功导出；独立审计确认 18/18 摘要匹配且 4 个拒绝 id 均未混入。 | App 0.5.7；533 JVM tests；`docs/P40_APP_0.5.7_NON_DEVELOPER_FLOW_VALIDATION_2026-07-18.md` |
| D-62 | 2026-07-18 | **AI 实时结论必须把“测量流程完成”与“用户业务任务成功”分层；缺指标不得吞掉已观察到的任务失败。** 若冻结证据包含意外会话中断或失败轮次，即使总分因必需指标缺失为 null，结论首屏也必须先报告受损会话/轮次，再给出会话中断率 ≤1%、音频帧返回率 ≥99% 的业务目标和本次差距。连接异常只能表述为活动网络切换、链路中断或节点关闭等候选原因；没有系统 PATH_CHANGE 冻结证据时禁止单因归因。P40 后台真实 Wi-Fi 中断 run 观察到 1/1 会话中断、2/3 轮失败、帧返回率 51.8%，结果页正确显示任务受损并抑制分数。 | App 0.5.7；534 JVM tests；run `019f7240-bf42-7a48-b23b-3235286da018`；`docs/P40_APP_0.5.7_NON_DEVELOPER_FLOW_VALIDATION_2026-07-18.md` |
| D-63 | 2026-07-18 | **三类正式结果必须冻结默认网络变化，但关联证据不得升级为因果结论。** 每个 run 注册 Android 默认网络 callback；稳定基线不生成变化事件，丢失、切换、恢复、VALIDATED 丢失/恢复和暂停/恢复去重写入统一 PATH_CHANGE 时间轴；新网络只有在已验证且未暂停时才写 ready。Android `Network` 句柄不得持久化，结果只使用每个 run 重新编号的 `path-1/path-2` 与承载。AI 实时在评分前先冻结同一份环境证据，保证结论所见与 Room/统一信封完全一致；连接异常与默认网络变化同窗时只允许写“共现、支持关联定位、不能单独证明因果”，UI 使用中文摘要、原始事件留在证据层。P40 最终 run 冻结默认 Wi-Fi 丢失、1/1 会话中断和 3/3 轮失败，当前 Schema/摘要均通过。 | App 0.5.7；539 JVM tests；run `019f72f5-557c-71b0-a7d9-b462055f0545`；`docs/P40_APP_0.5.7_NON_DEVELOPER_FLOW_VALIDATION_2026-07-18.md` |
| D-64 | 2026-07-18 | **发布后的结果 Schema 结构演进必须升版本并保留对应验证器；同名版本不得追溯收紧。** 最终批量导出的 27 条身份/摘要完整记录中，独立当前 Schema 审计发现 3 条 0.5.2/0.5.5 Token 历史缺少后来加入 v1 的 `task_id/server_processing_ms/ttft_ms`。这些旧信封不得回填、重算或删除；设备端完整性导出与离线 Schema 兼容性必须分层报告。后续新增 required 字段时发布新 Schema ID/版本，catalog 绑定版本化验证器和兼容窗口，历史记录按其生成时合同验证。 | 3 个历史 run；最终 JSONL 27/31、27/27 摘要匹配；`docs/P40_APP_0.5.7_NON_DEVELOPER_FLOW_VALIDATION_2026-07-18.md` |
| D-65 | 2026-07-18 | **结果合同采用“只做公共兼容约束的内部 core + 永久版本 wrapper”，新生产者发 v2，旧 v1 不重写。** `aneb-result-core-v1` 没有公开生产身份，只允许增加不改变既有 wrapper 接受集合的兼容约束；任何新增 required、收窄范围或改变语义都必须只进入新 wrapper。兼容 v1 接受发布早期不含任务对齐三字段和发布后期含可选三字段的两种不可变 Token 形状；严格 v2 要求每个 Token 任务包含 `task_id/server_processing_ms/ttft_ms`，三类 App 引擎从 0.5.8 起统一发 v2。设备 JSONL 导出同时支持 v1/v2，只验证身份与摘要，并把未来版本不支持和完整性异常分开；离线 `verify_result_jsonl.py` 再按每条记录版本执行结构验证。最终 27 条历史在未改写的情况下 27/27 通过兼容 v1；P40 新 v2 run 严格 Schema 零错误、3/3 任务字段完整、120 条无线样本和独立摘要匹配。 | App 0.5.8；541 JVM tests；run `019f730f-a0d5-7417-9e01-0866bacdfc57`；`docs/P40_APP_0.5.8_RESULT_VERSIONING_VALIDATION_2026-07-18.md` |
| D-66 | 2026-07-18 | **结论不得由导出器按列表位置猜语义，必须由各类独立评分器冻结稳定 ID、严重级别、文本和指标/证据依据。** Token、AI 实时和网络综合继续各自评分，但统一至少回答 verdict/置信度、业务任务完成性、Profile 业务行为、逐项网络目标和主要瓶颈；计划受控中断按恢复任务完成性评价，不冒充普通轮次失败。Compose 只展示同一冻结文本，不再维护另一套硬编码业务行为。由于结论输出语义实质变化，各结论策略和 12 个 Profile 均升小版本并重新绑定运行计划哈希；结果对象字段仍符合 `aneb-result-v2`，不为兼容字段新增无必要的 v3。 | App 0.5.9；Profile catalog 1.3.0；545 JVM tests；`docs/APP_0.5.9_SEMANTIC_CONCLUSION_VALIDATION_2026-07-18.md` |
| D-67 | 2026-07-18 | **E-01 默认测量入口改为 bare-IP 主通道，旧默认 SNI 偏好只迁移一次；接入失败与节点合同错误必须分开。** D-22 已把 bare-IP + IP-SAN 定义为蜂窝主通道，但持久设置仍默认 sslip.io。0.5.9 将旧默认精确迁移到 `https://120.79.148.0:8443`，用户仍可显式选 sslip.io 做 SNI 可达性对照。WebSocket 未收到 ready 只构成连接/任务失败，不再误报 `node_contract_mismatch`；只有收到非 v1 ready 才判合同错误。酒店 Wi-Fi 的两次失败发生于门户账号密码尚未认证，只证明未认证接入无法访问节点，不构成 8443 干扰或网络质量 A/B。0.5.9 紧邻候选的 P40 蜂窝 Quick 3/3 轮、strict-v2 JSONL 与语义结论通过；当前精确 APK 后续只调整失败门限 basis 与 Token 缺指标时的任务完成结论，真机同二进制确认待共享手机释放。 | `ProbeSettingsStore` 一次性迁移；`realtimeContractInvalidReason`；run `019f7377-9a61-7db5-a8c4-1ac57de1a486`；D-22/D-25；`docs/APP_0.5.9_SEMANTIC_CONCLUSION_VALIDATION_2026-07-18.md` |
| D-68 | 2026-07-18 | **系统下载导出必须服从“创建 pending → 完整写入 → 明确完成”事务边界；创建后的任何失败都尽力删除半成品，完成更新失败不得报成功。** 旧实现对 `openOutputStream=null` 直接返回且不删 pending 行，也忽略 `ContentResolver.update` 的返回值，可能留下不可用文件或误报成功。0.5.10 把生命周期提取为可故障注入的 sink，分别覆盖创建、打开、写入、完成和清理失败；清理失败时保留 URI 与明确错误供诊断，不伪装成已清理。该变化不改结果内容、摘要、Schema 或评分。 | `Exporter.exportWithSink`；`ExporterTest`；App 0.5.10；Profile catalog 1.3.1 |
| D-69 | 2026-07-18 | **云端 Debug 候选只能在全仓前置门禁全部通过后交付，且必须同时交付精确 APK、机器清单、SHA-256 和中文安装说明；Debug 永不冒充正式 Release。** 原 CI 只监听 main 且 Android job 只编译不上传，Codex 云分支不会执行、非开发者也拿不到产物。新 CI 监听 `main`/`codex/**`，Android 候选等待 contracts/server/gateway/behavior-model 全绿，再验证 Release 边界、APK ZIP、Gradle↔APK 身份、`com.aneb.probe.codex`、`-codex` 版本与 Android Debug 签名；非 PR 构建用 GitHub attestation 绑定来源，上传工件保留 30 天。任何身份不符、输出目录非空或文件缺失均 fail-closed。真实 run `29633753923` 的五个 job 成功，工件 `8426436270` 内外校验和与 APK 身份通过，云端 APK SHA-256 为 `2C05E347E66CC2049292452745DD68B6EDF2CECE2CB8501D509C4B9A6653DED1`，attestation `35942948` 已离线验真；外部固定 CA 网关命名空间步骤因无叶证书 secrets 明确未执行。 | `.github/workflows/ci.yml`；`scripts/package_debug_candidate.py`；20 scripts tests；GitHub 官方 upload-artifact/attestation 文档；`docs/CLOUD_DEBUG_CANDIDATE_DELIVERY_2026-07-18.md` |

| D-70 | 2026-07-18 | **公开仓不得依赖人工记忆防止凭据提交；本地和云端必须用同一高置信扫描器 fail-closed，日志不得回显匹配值。** 扫描范围固定为 Git 跟踪源码，覆盖 GitHub/模型供应商/云访问密钥、Slack Token 与 PEM 私钥头；路径越界、符号链接或跟踪文件缺失拒绝通过，二进制不作文本猜测。Android 云端候选依赖独立 security job。为避免白名单被滥用，不提供通用行级绕过；已有合成 Key 测试改为运行时拼接。扫描通过不代表已披露凭据安全，撤销与供应商审计仍是硬要求。 | `SECURITY.md`；`scripts/scan_repository_secrets.py`；`scripts/tests/test_scan_repository_secrets.py`；`docs/M4_CREDENTIAL_SAFETY_VALIDATION_2026-07-18.md` |
| D-71 | 2026-07-18 | **精确云端 Debug 候选的真机验收必须同时锁定来源、安装身份、历史数据保全和导出正文；不同 Debug 签名替换不得冒充原位升级或 Room migration。** source `51fdd7c81f1f63a7202dd40d8ce86f5931d0d1a2` 的 run `29635434193` 六个 job 成功，含 D-70 的凭据扫描；工件 `8427011992`、APK SHA-256 `49244B3157FCC47D54EDA61A51EAF4B69A71BD2B95314BAE54E327CE8B0F6D85`、attestation `35945988` / Rekor `2193995642` 已核验。P40 因新旧 Debug signer 不同，采用普通数据备份、卸载、安装、恢复；Room 仍为 v19、integrity OK，保全 36 条 `result_envelope` / 10 条 `test_run`，安全偏好/API key 明确不恢复。混合批量导出 32/36 条通过离线验证，v1=27、v2=5、三类结果合计 32 个唯一 run、重复 0；4 条历史完整性异常透明拒绝。单条 v2 run `019f7377-9a61-7db5-a8c4-1ac57de1a486` 与批次对应行逐字节一致，两条 MediaStore 行均 `is_pending=0`。这些事实只证明受控 Debug 成功路径与数据保全；失败分支仍由故障注入覆盖，非 ADB 用户安装链与正式签名 Release 仍未证明。 | D-61/D-65/D-68～D-70；`docs/CLOUD_DEBUG_CANDIDATE_DELIVERY_2026-07-18.md`；`docs/APP_0.5.10_EXPORT_RELIABILITY_VALIDATION_2026-07-18.md` |
| D-72 | 2026-07-18 | **M0-EC1 只为 Token Quick 冻结一个可证明的跨端执行合同，不以单个切片冒充全部 Profile 已统一。** `token_multimodal_quick@1.2.0` 以规范化 Profile SHA-256、`aneb-execution-requirements@1.0.0`、本地 `aneb-token-simulation-engine@1.0.0` 和服务端 `aneb-server-capability-receipt@1.0.0` 绑定 `echo/aneb-echo-v1`、`token_sim/aneb-token-task-v1`、`download/aneb-download-v1` 三项原语。P1 必须在首个业务请求前核对 APK 内 manifest、精确 Quick 身份/模式/目标/claim scope、合同版本、Profile 摘要与回执；Quick 缺字段、未知合同键、重复/缺失原语或版本/摘要冲突均 fail closed，业务请求为 0。其余 11 个 Published Profile 保持旧兼容路径；指标、门限、评分、Room 和结果合同均不改变。候选边界为 App 0.5.11 / server 0.8.0 / behavior model 0.3.0 / catalog 1.4.0，只有 E-01 与 P40 另行验收后才能写“已部署/已真机验证”。 | Product Owner 要求沿“3 子项目 + Profile”架构继续；`docs/M0_TOKEN_QUICK_EXECUTION_CONTRACT_VALIDATION_2026-07-18.md`；`spec/README.md` |
| D-73 | 2026-07-18 | **共享测试资源状态由执行方自动、原子地维护，Product Owner 不再手工改状态。** `SHARED_TEST_STATUS.md` 是 P40、ANEB、业务 App、VPN、PCAPdroid、E-01 与阿里云规则的唯一状态源；只有空闲可领取，领取绑定固定角色（Codex/Claude）和本次唯一 128-bit lease，旧任务或自由文本别名不得复用授权。当前执行方清理完成后自动转待交接；状态不明、回滚或清理失败自动转异常锁定；只有另一固定角色提交独立复核证据后才能释放为空闲。所有转换使用并发锁、基线比较和原子替换，部署工具必须精确核对本次 lease，不能要求用户代为编辑。 | Product Owner 明确要求 Codex 自动修改共享状态；`scripts/update_shared_test_status.py`；`SHARED_TEST_STATUS.md` 共同约定 |
| D-74 | 2026-07-18 | **用受限自动 `Verifier` 消除“另一开发方不在线就永久待交接”的活性缺陷，并替代 D-73 中“只能由另一固定角色释放”的限定。** Codex/Claude 仍是仅有的资源执行者；`Verifier` 不能领取、持有、清理或操作 P40/E-01，也不能执行普通交接、锁定或 lease 核验。它只可在状态为待交接时做两轮 T+0/T+10秒 只读复核：精确 P40、进程/服务/辅助功能、tun/VPN、前台与接入状态，以及 E-01 服务身份、H3、二进制、qdisc、规范化防火墙分量稳定性和 Phase 0 残留。复核开始即绑定状态文件原始字节 SHA-256；复核中状态变化不得覆盖，全部通过才执行待交接→空闲，任一确定失败执行待交接→异常锁定并保留原 owner/task/resources/lease。Codex/Claude 相互独立复核仍保留为自动复核不可用时的回退。该机制解决流程活性，不构成密码学身份隔离；未被当前只读复核 Profile 覆盖的资源不得自动放行。 | Product Owner 明确批准修改状态机；`scripts/update_shared_test_status.py`；`scripts/verify_shared_test_release.py`；状态原始 SHA 绑定与故障注入测试 |
| D-75 | 2026-07-18 | **AI 实时打断回归必须验证冻结的 300ms 业务合同，不能把“阈值后零在途帧”误写成协议要求。** 原 Go 测试用 5×20ms（总长仅 100ms）的响应并要求收到第 2 帧后总帧数精确等于 2：这既强于全双工网络可保证的语义，也因自然播放时长小于 LIVE-B08 目标而无法证明打断有效。修复后，服务端在绝对帧 deadline 逾期时仍先消费已排队 control，避免追赶发送跳过已经到达的 `barge_in`；确定性单测锁住该控制流。端到端测试改用 100×20ms 响应，并要求响应实际被截断、服务端/客户端帧数一致、`barge_in_received/protocol_ok` 成立、客户端单调时钟和服务端 stop anchors 均在 300ms 内。允许打断发出前已经写入/在途的额外帧，但不允许自然播完、漏账或超目标停止。 | D-41 / LIVE-B08；原门禁偶发 2→3/4/5 帧；确定性 red-green seam；500 次目标压力循环、server 全量 3 轮及全仓质量门通过 |
| D-76 | 2026-07-18 | **M0-EC1 的“可部署候选”必须证明来源、上传内容、staged/live 身份和同-run业务请求，单次 smoke 自洽不足以验收；本条取代 D-72 的 App 候选版本子句，D-72 的合同范围与非目标继续有效。** Linux 候选必须从精确 clean commit 的隔离 Git 快照构建，记录实际 Go toolchain、固定环境/flags、`go version -m`、binary 与全部上传工件摘要；部署端重新验证 manifest、严格能力回执和安装后完整工件身份，并把脱敏证据以 root-only 原子事务持久化。远端部署必须全生命周期互斥，私钥不得在清理保险丝建立前落盘，证据失败或回滚不完整不得输出成功。App 0.5.12/code 44 为同一 Token run 的 reachability、能力回执、echo、token-sim 和 download 绑定 `X-Aneb-Run-Id`；服务端只异步记录净化后的固定字段，不能把日志 I/O 写入 Echo RTT。用日志缺失证明零请求时，必须绑定 run 起止边界、观察到 run 后专用 `/serverinfo` barrier，并确认窗口起点到 barrier 之间无 drop 记录；否则只能判证据不完整。历史 0.5.11 负向结果仍只能证明客户端 fail closed 和无客户端业务产物。 | App 0.5.12；server 0.8.0 候选；`scripts/build_server_candidate.py`；`scripts/deploy_server.ps1`；`docs/M0_TOKEN_QUICK_EXECUTION_CONTRACT_VALIDATION_2026-07-18.md` |
| D-77 | 2026-07-18 | **受限 Verifier 的配置错误属于不确定性，必须在任何探针前拒绝，不能把调用方短值变成设备异常。** 交接资源仅接受当前 Profile 精确覆盖的 `P40 Pro/ANEB Codex/ANEB Claude/E-01` token；预期服务版本必须使用完整 `aneb-server/<semver>` 身份，摘要必须为完整 SHA-256。P40/SSH/解析/状态写入等不确定失败保持待交接，只有明确脏态才锁定。E-01 v2 探针同时覆盖 Phase 0 unit enabled、接口、规则、端口及精确 `/etc`、`/opt`、`/run`、`/var/lib` 路径；本轮 v2 探针发现两个先前漏掉的空运行态路径，随后由原执行方 Claude 受控清理，Verifier 自身未执行清理。 | D-74；23:12 T+0/T+10秒 自动释放；`scripts/verify_shared_test_release.py`；`docs/SHARED_RELEASE_VERIFIER_VALIDATION_2026-07-18.md` |
| D-78 | 2026-07-19 | **Profile 声明的必需原语必须在该 Profile 的真实运行计划中发生，不能用手工 transport 测试冒充业务覆盖。** 红队发现 `token_multimodal_quick@1.2.0` 声明 `download`，但生成器按每类最短任务选择出的三项 `response_artifact_bytes` 全为 0，导致真实 App 不会下载而正向验收必然失败。禁止手改 published JSON；P3 0.3.1 改为在保留每类一个任务的同时，若最短集合无返回附件，就从同一模型与 seed 的计划中选择字节最小、时长最短的附件任务。当前确定性选择 task-0006（text、1MiB），并将不可变身份升为 Quick 1.2.1、catalog 1.4.1；Profile/runtime/manifest 必须由 `publish-runtime` 重生成且字节可复现。D-72 的 1.2.0 身份成为历史，合同范围与非目标不变。 | P3 0.3.1；`generator.py`；Quick 1.2.1 manifest；生成器与 App 发布包回归 |
| D-79 | 2026-07-19 | **同-run日志的 barrier 只有在请求进入时先非阻塞入队、使用固定无前缀 grammar 且窗口内无未归因业务时才可证明计数。** 原 defer 入队允许流式业务响应完成后 barrier 反超；默认 Go logger 又会添加日期前缀。修复为 handler 前入队、专用无前缀 logger、固定 `GET/POST/OTHER` 方法集合和可观测 DROP。独立判定器要求唯一 run 后 barrier；任意 DROP、畸形行、目标合同外 method/path，或窗口内 legacy/invalid/其他 run 的 business 都 fail closed。负向必须全窗口 business=0；正向必须目标 echo/token-sim/download/control 齐全且无未归因业务。 | D-76；`server/request_audit.go`；`scripts/verify_token_run_audit.py`；并发/噪声/格式 TDD |
| D-80 | 2026-07-19 | **Product Owner 取消基于 `SHARED_TEST_STATUS.md` 的共享状态、lease、待交接和受限 Verifier 机制；本条取代 D-73、D-74 与 D-77 的现行操作规则，但不改写其历史事实。** Codex/Claude 以 P40 实时现场作为接管门禁：设备在线、Huawei Launcher 前台且无活动 ANEB/目标业务 App/VPN/tun/抓包才可开始，干净则无需 claim 或二次放行；无法安全归属的既有会话不得擅自停止、覆盖或清理。任务结束必须停止本轮全部相关 App、VPN、抓包及临时网络规则，恢复临时设置、返回 Launcher 并立即复核。桌面不是残留清理的充分证据，无法确认时停止后续测试并报告。E-01/阿里云不再接收共享 `LeaseId` 或状态文件授权，但仍必须执行独立受保护预检；E-01 在任何 live 变更前取得远端 `/run/lock/aneb-deploy.lock` 内核 `flock`，受限变更失败时原子回滚，并保留来源、快照和验后证据门禁。旧 updater/Verifier 保留为固定非零退出且不探针、不写状态的兼容薄壳，防止缓存调用静默复活旧流程。 | Product Owner 明确要求取消低效共享文件机制；`scripts/deploy_server.ps1`；退役薄壳及回归测试 |
| D-81 | 2026-07-19 | **M0-EC1 同-run审计升级为有界、连续、可归因的 request-entry 证据合同；本条取代 D-79 的单一 run 后 barrier 与窄入口判据，D-79 仅保留历史。** `X-Aneb-Run-Id` 只接受规范小写连字符 UUID 文本（P1 生产 run 为 UUIDv7），目标 run、window start 和 window end 使用三个互不相同的规范 UUID；`X-Aneb-Audit-Role` 是固定枚举 `reachability/capability/window_start/window_end`，缺失按端点语义处理，未知值或重复头一律归一为 `other`，不记录不可信原值。每条审计固定输出 `instance_id`、`seq`、`class`、`method`、`path`、`role`、`scope`、`run_id`；判定窗口必须有唯一 start/end barrier、同一 `instance_id`、严格连续递增 `seq`，任一 drop、重启、缺号、重复或乱序均 fail closed。审计中间件必须覆盖并规范化所有直接 `/api/v1/*` 及 `/synthetic/.../api/v1/*` 请求入口；未知方法/路径归为 `other` 而非静默漏记，query/body/远端地址不入日志。该日志只证明请求已进入服务端审计边界及窗口内的 request-entry coverage，不证明 handler 完成、响应成功、响应体传输、客户端接收或评分正确；正/负向验收必须与同一 run 的客户端冻结结果及节点身份证据结合，任一侧缺失只能判证据不完整。部署门禁还必须让本地测试、候选构建、catalog/Profile/manifest 与上传部署绑定同一个精确 clean commit，并在测试与构建两处使用同一净化 Go 环境（至少 `GOENV=off`、`GOWORK=off`、空 `GOFLAGS` 及冻结的目标/工具链变量）；提交或环境不一致不得部署或写 PASS。本条不改变 KPI、AQS、T4、任何阈值、Room/结果合同或失败值的 `null` 语义。 | D-76/D-79 后续红队；`server/request_audit.go`；`scripts/verify_token_run_audit.py`；`scripts/deploy_server.ps1`；Android 审计头传递回归 |
| D-82 | 2026-07-19 | **D-81 判定器只验证提供给它的日志内容，不单独证明记录属于“本次”验收；正式现场证据必须另行绑定新鲜度与原始来源。** App 为本次运行生成新的目标 run UUID，控制器独占生成新的 start/end UUID，三者都不得从历史证据复用；start 前冻结 journal cursor、boot ID、systemd invocation ID、MainPID、节点身份和本地时间，窗口期间持有与部署互斥的 E-01 审计锁；保存 start/end 原始 HTTP 头与 body，并从 pre-start cursor 导出保留 cursor/realtime/unit/PID/message 的原始 journald JSON。判定报告必须包含固定 schema/version、派生 message 日志 SHA-256 与字节数；最终证据 manifest 同时哈希 pre-start receipt、原始 journald JSON、派生日志、判定报告、原始 serverinfo/barrier 响应和客户端冻结结果。缺少独立 pre-start receipt、锁、原始 JSON 或任一摘要时，只能声明“提供的文本在内容上通过”，不得声明本次现场验收或零请求。 | D-81 红队重放反例；`verify_token_run_audit.py` v2 报告；M0-EC1 现场证据包 |
| D-83 | 2026-07-19 | **Go 候选的可复现环境必须显式冻结 `GOFIPS140=off`。** Go 1.26 实测仅把宿主 `GOFIPS140` 从 `off` 改为 `latest`，同源码和同 flags 即生成不同 linux/amd64 二进制；旧来源证明未记录该维度。构建器、部署前 Go 门禁、provenance 和远端复核统一固定 `off`，宿主污染必须被覆盖并在结束后恢复；buildinfo 显式值若存在必须为 `off`，缺省按 Go 的 `off` 语义处理。该项只冻结构建身份，不改变服务协议、Profile、KPI、AQS、T4、阈值或 `null` 语义。 | Go 1.26.5 双构建反例；`build_server_candidate.py`；`deploy_server.ps1`；污染环境回归 |
| D-84 | 2026-07-19 | **Token Quick 正向 request-entry 证据必须精确验证 20 次 echo、3 次 token-sim 和 1 次 download，不能把“每类至少一次”冒充冻结执行合同。** 这三个次数不是 Profile 的直接基数字段：20 来自生产 Engine，3/1 来自冻结 runtime plan；统一 sidecar 绑定 Engine 身份、Quick 1.2.1 固定路径与 Profile/runtime 摘要，Kotlin 漂移测试再交叉验证生产常量、任务数和附件任务数。catalog 1.5.0 正式登记该资产并冻结其 `canonical-json-sha256-v1`；原始文件字节摘要由 D-82 manifest 单独保存，二者不得混用。判定器只从该 sidecar 读取次数，且仅在显式传入 `token_multimodal_quick@1.2.1` 时启用；sidecar 缺失/畸形 fail closed，不带合同参数的通用模式保持向后兼容。报告升级为 v2.1.0，并机器可辨地记录合同身份、规范化定义摘要、期望计数和正向精确/负向零业务语义；非法合同不回显，非字符串库输入也返回规范失败报告。精确计数只证明请求进入审计边界，仍须和同 run 客户端 completed 结果、D-82 新鲜度与原始来源证据结合。该项不改变业务流量生成、Profile、KPI、AQS、T4、门限或 `null` 语义。 | `spec/execution-contracts/token_multimodal_quick-1.2.1.request-entry.json`；Quick 1.2.1 runtime plan；`TokenSimulationEngine`；`verify_token_run_audit.py` v2.1.0；Kotlin/Python 漂移与故障注入 TDD |
| D-85 | 2026-07-19 | **D-82 正向现场证据只有在独立复核原始来源、客户端 Room 语义和生产评分后才能发布为 `.complete`；采集器自己的成功状态不能自证。** 采集必须来自 clean commit、仓库外当前用户私有目录，并逐项绑定最终 manifest 覆盖的实际执行代码/合同输入、外部身份锚、固定 CA、精确 APK 与 E-01 身份。最终包先在私有 verification-stage 中重派生 journald MESSAGE、重跑 20/3/1 request-entry 判定、核对三次 serverinfo 时间/启动/Profile、验证 Room v19/WAL/SHM 冻结清单，并依据生产算法重算 14 个 typed 指标、26 个 envelope 指标、组分、总分、等级、覆盖率、置信度与结论；同步伪造分数/结论或替换引用均 fail closed。只有独立 verifier 在同一进程完成末次重哈希和原子 rename 后才发布最终目录及 sibling report；失败保留 `.verification-failed.partial`，不得短暂暴露伪 `.complete`。这仍是可信采集主机与获授权操作者假设下的内部一致性证明，不是对恶意宿主或物理攻击者的远程证明。本条只增加验收证据强度，不改变 KPI、AQS、T4、任何门限/权重或 `null` 语义；当前工具只覆盖正向 completed/valid，负向必须另行实现稳定机器原因落库与不修改 E-01 的受限代理。 | `scripts/collect_token_quick_evidence.ps1`；`scripts/verify_token_quick_client_db.py`；`scripts/verify_token_quick_evidence_bundle.py`；D-82 联合回归与全仓质量门 |

| D-86 | 2026-07-22 | **M0-EC1 负向验收采用一次性本机 loopback 代理制造 `receipt_missing`，不得修改 E-01，也不得把代理完成冒充客户端已接收。** 仅 Debug 验收模式允许控制器通过固定设备端口、动态主机端口和精确 `adb reverse --no-rebind` 将 App 的 loopback `/serverinfo` 请求送入一次性代理；E-01 身份、start/end barrier 与审计仍直接使用固定 CA 的 HTTPS。代理只能删除 serverinfo 顶层 `execution_capabilities`，其余字节语义、上游 URL、CA 摘要、run、端口、请求账本和 stdout/stderr 必须进入证据并由独立验证器重算；代理 receipt 必须保留 `client_delivery_proven=false`。客户端只有在业务请求前把机器原因 `receipt_missing` 持久化、输出冻结顺序 `START → RADIO → DB_WRITE(ok=true) → CONTRACT(rejected,receipt_missing) → END(contract_rejected)`、Room 中任务/KPI/网络产物为零且 score/grade 为 null、服务端窗口业务入口为零时，才可判负向合同通过。最终 manifest/plan/report 以版本化 `execution_mode=positive|negative_receipt_missing` 和不同 evidence scope 绑定模式，负向文件使用精确允许集合；reverse/proxy 必须按本轮映射精确清理，禁止 `--remove-all`，清理不确定即 fail closed。P40 实时协调还要求干净预检后立即以可见 Settings 哨兵占住现场，远端准备期间不得仅留 Launcher 造成“空闲”误判，目标 App 停止后恢复哨兵，最终清理并返回 Launcher。该模式只验证 Quick 的受控缺回执拒绝路径，不改变 Profile、KPI、AQS、T4、门限、权重或 `null` 语义。 | Product Owner 批准推荐负向方案；D-72/D-76/D-80/D-82/D-85；`token_serverinfo_negative_proxy.py`；负向 Room/collector/bundle TDD |
| D-87 | 2026-07-22 | **D-82/D-86 证据只有在摘要绑定的 sibling `READY.json` 最后提交后才算已发布；单独存在 `.complete`、`COMPLETE` 或 verification report 都不是验收成功。** 目录与 sibling 文件无法用两次 rename 获得物理事务，因此采用逻辑提交点：独立 verifier 先发布已复核 bundle，采集器严格绑定单行 report 后以 CreateNew+flush 写临时 report 并原子改名，再重算 final manifest/report SHA-256，以 CreateNew+flush 写 `<collection>.ready.partial`，最后 no-replace 改名为 `<collection>.READY.json`，之后才允许 `Published=true`。受控失败必须删除本轮 report、移除 bundle 内 `COMPLETE` 并降级为 `.verification-failed.partial`；进程崩溃遗留但缺 READY 的文件只能诊断或隔离，消费者不得自动补 READY。EvidenceRoot 同时要求当前用户为 owner，写权限只允许当前用户/SYSTEM/Administrators，并在创建前、创建后及发布前逐级拒绝任一父级 reparse point，不能用词法路径绕过仓库外私有目录边界。Room 冻结仍采用主 DB 必有、WAL/SHM 同为存在或同为缺失的统一合同；不得把合法 checkpoint 后 paired-absent 误判为负向失败。该项不改变 Profile、KPI、AQS、T4、门限、权重或 `null` 语义。 | D-82/D-85/D-86 安全复核；collector READY/ACL/reparse TDD；正负 bundle paired-sidecar 故障注入 |
| D-88 | 2026-07-23 | **云端 Debug 候选的 provenance 必须同时覆盖 APK、机器 build manifest 与中文安装说明，不能只签 APK 后再允许未签载荷进入候选。** 非 PR CI 先生成精确三项 `checksums.sha256`，以 `actions/attest@v4 subject-checksums` 产生单份多 subject 证明，再只允许追加一份 provenance bundle 并原子重建精确四项最终清单；独立 verifier 必须让 gh statement 的 subject 名称和 SHA-256 全集与当前三项逐一相等，额外/缺失/重复 subject、额外载荷、直接或祖先 symlink/junction/reparse 全部 fail closed。`id-token`、`attestations`、`artifact-metadata` 写权限只授予 Android job；PR 工件明确标记 review-unattested，不能冒充已验签候选。该合同增强供应链闭环，但仍服从 D-85 的可信 GitHub-hosted runner/固定 workflow/repository 边界。 | CI/packager/provenance verifier TDD；官方 `actions/attest@v4` 多 subject 合同；独立安全复核 |
| D-89 | 2026-07-23 | **Token 能力合同拒绝结果必须冻结实际请求端点，并把全部 post-commit 终态日志放在“已持久化”保护区。** SNI reachability 旁路到裸 IP 时，失败发布器不得另持 configured base；runner 将执行合同所用的同一 `serverBase` 直接传入持久化。`RADIO → DB_WRITE → CONTRACT → END` 四条落库后日志任一非取消异常都只能抛出 `token_contract_rejection_already_persisted`，不得回到通用失败路径二次插入同一 run。该修复不改变 Profile、KPI、评分、门限、权重或 `null` 语义。 | 两条 RED→GREEN durability 测试；SNI→bare-IP endpoint 回归；相关 scorer/envelope/gate/reachability 回归 |

> ［KNOWN｜HIGH］D-86 只取代 D-85“工具当前仅覆盖正向”的子句；D-87 只取代 D-85“`.complete` 即为发布终态”的子句。D-85 的 clean commit、私有证据根、独立重算和失败降级要求继续有效，原文保留为历史决策。
>
> ［KNOWN｜HIGH］D-87 消费者除重算 READY 中的两个 SHA-256 外，还必须严格解析 final manifest 与 verification report，四方绑定 collection/run/mode/manifest SHA，并复核 Profile scope、正负计数、source/server/APK/device 身份及关键 reverified/recomputed 标志。它验证发布字节与语义闭环，不声称事后证明 EvidenceRoot 的历史 ACL，也不防御恶意本机、内核或物理攻击者；这些边界仍服从 D-85 的可信采集主机假设。
>
> ［KNOWN｜HIGH］D-73、D-74 与 D-77 仅保留 2026-07-18 的历史决策链，均已被 D-80 superseded；
> 其中的状态领取、lease、待交接、异常锁定和自动释放要求不得继续执行。

| D-90 | 2026-07-27 | **Network Quick 1.2.0 的 UDP 应用探针升级为 `aneb-udp-echo-v2`，每个数据报冻结当前 run UUID、序号和单调发送时间；服务端 0.8.2 精确声明 v2，同时仅为旧客户端兼容继续回显合法 `ANEB1` 报文。** v2 布局固定为 `ANEB2`（5 bytes）+ RFC 4122 UUID（16 bytes）+ big-endian sequence（4 bytes）+ monotonic timestamp（8 bytes）+ 有界填充，最小 33 bytes、最大 512 bytes；服务端只原样回显且不放大。客户端必须拒绝非本 run UUID 的回包。该变更只增强 Network Quick 同-run 归属，不改变 NET-B10/NET-B11 的“应用数据报未返回/乱序”口径，不得写成 IP 层丢包。旧 Profile 评分、门限、权重和历史结果不重算。 | Product Owner 已授权按推荐方案自主推进各阶段；M0-EC3 scope；Network Quick 1.2.0 runtime/evidence contract；P1/P2 UDP packet tests |

| D-91 | 2026-07-27 | **Network Quick 的 `aneb-udp-echo-v2` 服务端接收必须在回显前进入与 HTTP 相同的有界 request-entry 审计 FIFO；每条 UDP 审计冻结 `network_run`、规范 run UUID、数据报序号和字节数。** 独立 UDP 监听与 H3 共端口过滤路径使用同一进程 `instance_id/seq`；合法旧 `ANEB1` 只做无归属兼容回显，不得进入 Network Quick 同-run 计数。正向判定要求唯一 capability 在业务前、HTTP 至少 18 echo/4 download/2 upload、UDP 恰好 50 条且序号为 0..49、每条 256 bytes；负向允许 capability control，但所有 HTTP/UDP 业务均为零。任一 drop、序号缺口、进程实例变化、并发窗口、未归属业务、目标窗口外记录或合同摘要漂移均 fail closed。该证据只证明请求/数据报进入服务端审计边界，不证明回显已被客户端接收，也不把应用数据报未返回写成 IP 丢包；必须与同 run 的冻结 Room 结果及节点/APK provenance 联合。此项不改变 KPI、AQS、T4、门限、权重、Room schema 或 `null` 语义。 | D-81/D-90；`server/udp_echo.go`；`server/request_audit.go`；`scripts/verify_network_quick_run_audit.py`；UDP dedicated/shared integration tests |
| D-92 | 2026-07-27 | **Network Quick 的客户端 Room 判定、服务端 request-entry 判定与 App 终态日志必须按同一 run、同一 mode 和同一 Profile 合同交叉绑定；任一判定失败、跨 run、跨模式、正向零业务或负向非零业务均 fail closed。** 该适配层只重算并绑定已经冻结的证据，不自行证明手机生命周期、APK/服务器 provenance、采集新鲜度、清理或 READY 发布；完整设备采集器和 READY consumer 在实现并实测前仍记为缺口。此项只增强证据拼接边界，不改变 Profile、KPI、AQS、T4、门限、权重、Room schema 或 `null` 语义。 | D-81/D-85/D-87/D-91；`scripts/collect_network_quick_evidence.py`；Network collector contract TDD |
| D-93 | 2026-07-27 | **三类 Quick 只共享 fail-closed 的机械生命周期顺序，不共享或隐式继承业务身份。** 共用 workflow 固定为 `preflight → acquire → collect → phone/remote cleanup → publish`，只有采集成功且两侧清理均成功才允许发布；主失败、每个清理失败和发布失败分别保留。Realtime 与 Network 使用不可混用的不可变身份合同，分别冻结 Profile、App/server 版本、APK 名、audit scope 与证据 schema；Network 另行要求 0.8.2 的六原语/三 Profile serverinfo 和三次稳定、按时序递增的身份快照。Network 设备机械后端必须调用自身终态解析、审计头、serverinfo、三判定器及发布钩子；在 Network bundle verifier/READY publisher/consumer 完成前，发布钩子必须明确 fail closed，不得退回 Realtime 语义。该抽取和机械接入不自行构成 bundle、READY 或 P40 验收完成。 | D-85/D-87/D-92；`scripts/quick_collection_workflow.py`；`scripts/quick_collection_contract.py`；`scripts/collect_network_quick_evidence.py`；workflow/identity/serverinfo/backend/report TDD；完整质量门 783+44 PASS |
| D-94 | 2026-07-27 | **Network Quick 的证据发布必须由 Network 自己的离线 consumer 完成，不能调用或改写 Realtime 的业务判定。** 采集时把精确 Profile/runtime/manifest 三文件冻结进 bundle；collection verifier 重新验证 manifest、候选 provenance、设备/远端基线、Network 0.8.2 serverinfo 序列，并重新计算客户端 DB、服务端 request-entry 与 App 终态三份报告后逐份比对；publisher 先验证 partial，再无覆盖原子提交 complete/verification/READY；release consumer 校验摘要并再次重算整包。Network 与 Realtime 的远端 marker 分别固定为 `aneb-network-audit-*` 与 `aneb-realtime-audit-*`；Network 必须有独立 CLI/preflight，不能要求调用者手工导入后端。该离线链通过不等于 P40 正负 READY；没有真实 bundle 时所有真机状态仍为未完成。 | D-92/D-93；`scripts/collect_network_quick_evidence.py`；`scripts/verify_network_quick_collection.py`；`scripts/publish_network_quick_ready.py`；`scripts/verify_network_quick_release.py`；Network/Realtime release 回归；完整质量门 790+44 PASS |
| D-95 | 2026-07-27 | **共享 Quick 机械后端中的类别身份必须全部来自不可变合同，独立 Network consumer 还必须重验采集新鲜边界。** Network 与 Realtime 分别冻结 audit scope、busy-sentinel schema 与启动操作码；end-barrier 只能等待本类别 scope，busy sentinel 不得接受另一类别 schema；Room 冻结复制必须显式使用当前 backend 合同包名，不得读取 Realtime 模块默认值。Network consumer 要求三阶段 Settings sentinel、唯一 D82 capture marker，并只解析 marker 后日志；`app-logcat.stderr.txt` 必须存在且为空。任何跨类别默认值、旧终态日志或 logcat stderr 都 fail closed。该硬化不改变 Profile/KPI/评分，也不替代 P40 正负 READY。 | D-93/D-94；shared backend identity TDD；Network/Realtime 交叉回归 107/107；Room 包名去耦后分段完整质量门 796+44、Android、发布边界、secret scan、候选封装、spec/result schema、server/gateway Go 全部 PASS |
| D-96 | 2026-07-27 | **Network Quick 的独立 CLI 必须由真实子进程直接启动验证，不能只依赖模块导入单测。** 包导入与 `python scripts/collect_network_quick_evidence.py` 直接执行使用显式 `__package__` 分支；不得以捕获 `ModuleNotFoundError`/`ImportError` 猜测执行形态，因为环境中同名 `scripts` 模块会把真实入口变成 `--help` 即失败。任何候选即使 CI/provenance 正确，只要直接 CLI 入口不可执行，就不得进入 P40。 | D-94/D-95；direct CLI subprocess TDD；Network/Realtime 14 模块交叉回归 159/159 PASS |
| D-97 | 2026-07-27 | **M0-EC3 只在同一 source/CI APK/signer/server/Profile/runtime 上同时取得正向与负向 READY、独立 consumer 重算和最终清理后结案；Quick 分数不得折算为正式网络质量基线。** 最终候选为 source `ea9de17c2acea763513b144b4fb9942a3d54c5c6` / CI `30266912724` / APK `e1af670c…db0e`。权威正向完成四原语且 cross-bound authorized；负向 `receipt_missing` 在首业务包前拒绝且两端业务为零。两轮均原子发布 READY 并独立复算；P40/reverse/代理/远端锁最终清理。权威正向 `79/B` 仍受 `INCONCLUSIVE/LOW`、coverage 0.5 限制。首份正向本地 bundle 被普通 SQLite 读取触发 checkpoint 后，consumer 以 manifest binding mismatch 拒绝；该旧 READY 已废弃并以同候选单次恢复采集替换，不重建证据。由此只关闭 Network Quick 的跨端执行合同，并进入 S2 机械生命周期收敛，不关闭 Standard/Recovery、正式重复性、外场或发布里程碑。 | `M0_EC3_NETWORK_QUICK_READY_VALIDATION_2026-07-27.md`；正向/负向 READY；PhoneGuard 与 E-01 final receipts |
| D-98 | 2026-07-27 | **S2 的第一个深模块只统一 Quick READY 的机械发布/消费事务，不统一业务语义。** `QuickReadyContract` 冻结 family schema/路径身份，`QuickReadyAdapter` 只把私有根与 collection 业务重算接入；canonical JSON、path/reparse、exclusive+fsync+no-replace、摘要绑定、postcheck、重算与回滚归共用模块。Realtime/Network 保留薄 CLI/异常/schema；Token 暂不迁移，只作为真实 READY 兼容对照。操作者中断必须在清理新 sibling 后原样传播。 | `S2_READY_TRANSACTION_CONTRACT_2026-07-27.md`；`scripts/quick_ready_transaction.py`；模块/原回归/direct CLI/六份冻结 READY 等价性 |
| D-99 | 2026-07-27 | **S2 verifier 中立化必须先切断底层原语，再迁移高层机械合同，禁止把 Network→Realtime 私有函数继承包装成“共享模块”。** 第一层 `quick_collection_verifier.py` 只拥有文件/JSON/摘要/路径/UUID/endpoint/component 原语，零业务家族 import；Realtime/Network 聚焦回归保持原 reason code。candidate/manifest/phone/remote/lock/serverinfo/COMPLETE 尚未迁移时，里程碑只记 `S2-V1`，不得宣称 verifier 收敛完成。 | `S2_READY_TRANSACTION_CONTRACT_2026-07-27.md`；`scripts/quick_collection_verifier.py`；三模块 26/26 回归 |
| D-100 | 2026-07-28 | **S2 高层 collection verifier 机械逻辑必须由不可变 Adapter 合同驱动；Network 不得再导入 Realtime collector/verifier。** `QuickCollectionVerifierAdapter` 拥有 manifest、candidate provenance、phone pair、device identity、remote snapshot、lock、serverinfo、mode inventory、evidence-root 与 COMPLETE 的共享算法；Network 只注入自身 schema/候选身份/marker/serverinfo 与安全验证回调，业务 cross-evidence 重算仍留在 Network。历史 `verify_realtime_evidence_security` 命名尚未中立化，且 Realtime 尚未切换到 Adapter，因此本里程碑只记 `S2-V2`，不宣称三族 verifier 全部收敛。 | `scripts/quick_collection_verifier_adapter.py`；Network 零 Realtime collector/verifier 静态门；跨族 29/29；release 12/12 |
| D-101 | 2026-07-28 | **S2 证据根安全必须是业务族中立实现，Realtime 与 Network 的高层 verifier 机械必须由同一不可变 Adapter 驱动；历史命名入口只能保持同一模块身份的兼容，不得复制状态。** `quick_evidence_security.py` 成为生产实现；旧 `verify_realtime_evidence_security` 通过 `sys.modules` 映射到同一模块，以保留旧 monkeypatch/ACL/POSIX/path-binding 语义。Realtime 删除与 Adapter 重复的 manifest、candidate、phone、device、remote、lock、serverinfo、inventory、security 与 COMPLETE 算法，只保留自身 plan/status/run 与 cross-evidence 业务判定。此里程碑记 `S2-V3`；Token 尚未迁移，因此不得宣称三族 verifier 已收敛。 | `scripts/quick_evidence_security.py`；`scripts/verify_realtime_quick_collection.py`；compatibility 3/3；security 9/9；双族 collection 30/30；最终 production 97/97 |
| D-102 | 2026-07-28 | **共享 READY 事务必须用不可变合同表达各族 mode 字段和值域，不能要求 Token 改写历史 `execution_mode`，也不能把 Realtime/Network 的 `mode` 放宽为任意字符串。** `QuickReadyContract` 新增 `mode_field/mode_values`；默认仍是 `mode={positive,negative}`，Token 兼容方言精确为 `execution_mode={positive,negative_receipt_missing}`。mode key 碰撞 READY 身份字段或值不在白名单时 fail closed 并回滚新 sibling；公开 `READY_KEYS` 默认兼容面继续保留。此项只建立 S2-V4a 兼容切口，Token publisher/consumer 尚未迁移。 | `scripts/quick_ready_transaction.py`；Token mode RED→GREEN 6/6；Realtime/Network release 18/18 |

## 否决记录（评估后明确不采纳）

- **Cronet 逐请求 bindToNetwork**：OkHttp 主栈无此 API；改用 `requestNetwork` + `socketFactory`/`Dns` 双绑定，保留其 fail-closed 语义（绑定不可得即不出数）。
- **M2 式里程碑制与正式 PO 角色**：研究者本人即决策者，轻量四态证据 + 阶段验收足够；对方被外部依赖全线卡死（M2=NO）正是过度制度化+外部前置的后果。
- **功能范围冻结**：本项目冻结的是 profile 与 KPI/AQS 的版本语义，不是功能范围——流式/上行恰是主战场。
- **单指标名 const 锁死**：本体系多 KPI 且版本化演进；schema 锁 claim_scope 与版本字段，不锁指标名清单，否则与"门限随数据回流重标定"（D-08 配套）冲突。
- **最小权限清单照搬**：R 组无线层归因必须采集小区/信号，申请 `ACCESS_FINE_LOCATION`+`READ_PHONE_STATE`，差异与数据保护承诺显式声明（设计文档 §9.1）。
- **invalid 全抑制到数据层**：抑制只作用于 KPI/AQS 聚合层；原始事件全量保留（取证需要分析失效原因）。
- **设备矩阵/双节点实验室校准作为验收前置**：留待阶段三按需借用其判据框架（同条件 median CV≤8%、同 CDN 边缘不算独立节点），不前移为阶段 0–2 门槛。
- **对方 Echo RTT 数据直接当 N1 基线**：Cronet vs OkHttp 栈间系统差未标定前，两项目数据不可直接互比；互校须同设备同网络先标定栈差。

## 外部依赖清单

原则：每项外部依赖必须有本地替代方案，保证**每个阶段都有纯本地可完成的验收路径**；依赖缺位时对应检查记 `BLOCKED_EXTERNAL`，绝不折算成 PASS。

| ID | 依赖 | 最晚需要时点 | 本地替代方案 |
|---|---|---|---|
| E-01 | 国内云 VM（2C4G、公网 IP、8443/TCP，阶段二 +UDP） | 阶段 0 后半（真机联调） | 局域网 Linux 盒 / WSL2 + tc netem |
| E-02 | Android 真机 + 蜂窝 SIM（4G/5G） | 阶段 1 蜂窝验收 | 模拟器只能产出功能/fail-closed 兼容性证据，不构成测量证据（参考项目教训：模拟器无 NET_CAPABILITY_VALIDATED） |
| E-03 | 真实 LLM API key（Anthropic / Kimi） | 阶段 2 探针 | 跳过探针对照列，主线不受阻 |
| E-04 | 海外节点 | 阶段 3 | netem 模拟跨境 RTT/丢包剖面 |
| E-05 | CAMARA QoD 试点（运营商合作） | 阶段 3 | 无替代；记 BLOCKED_EXTERNAL |
| E-06 | 域名 + 公共 CA 证书（Let's Encrypt，绑定 E-01） | 阶段 2 云端 QUIC A/B（P2-C06） | 无替代（D-21：Cronet QUIC 强制公共已知根，自签不可行）；同批次需 UDP 8443 放行 |
