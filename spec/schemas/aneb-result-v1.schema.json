{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:aneb:schema:result:v1",
  "title": "ANEB unified measurement result compatibility v1",
  "description": "Backward-compatible validator for every published aneb-result-v1 envelope. Task-aligned TTFT fields remain optional because they were added to the producer after v1 was already published.",
  "properties": {
    "schema_version": {
      "const": "aneb-result-v1"
    }
  },
  "required": ["schema_version"],
  "allOf": [
    {
      "$ref": "urn:aneb:schema:result:core:v1"
    }
  ]
}
